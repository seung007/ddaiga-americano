# 설정 안내 — 2026-09-06

오늘 붙인 것들과, **hyun님이 직접 해야 하는 설정**을 정리합니다.
코드는 다 들어갔지만 **키와 알림은 사람이 발급/설정해야** 켜집니다.

---

## 1. 오늘 붙인 것

### ① 글 안에서 바로 질문하기 — `InlineAsk`

**왜 만들었나.** `/community`는 두 달 넘게 질문이 **0건**이었습니다.
페이지가 고장난 게 아닙니다 — 라이브에서 확인했고 정상 작동합니다.
문제는 **거리**였어요. 네이버 검색으로 오는 46명은 `/injury/*`에 도착하는데,
거기서 Q&A까지 가려면 **클릭 한 번**이 필요합니다. 그 한 번에서 대부분을 잃습니다.

그래서 질문창을 **사람이 이미 서 있는 자리로** 옮겼습니다. 클릭 0회로 질문됩니다.

**들어간 곳 (6개)**

| 페이지 | 저장되는 태그 |
|---|---|
| `/injury/first-10k` | 기타 |
| `/injury/wide-foot` | 발볼 |
| `/injury/cooldown` | 기타 |
| `/injury/achilles` | 아킬레스 |
| `/injury/knee-pain` | 무릎 |
| `/injury/plantar-fasciitis` | 족저근막 |

기존 `community_posts` 테이블을 그대로 씁니다. **새 테이블도 마이그레이션도 없습니다.**
`/community` 페이지도 그대로 살아 있고, 여기서 올린 질문이 거기에도 뜹니다.

**어느 글이 질문을 만드는지**는 GA 이벤트 `inline_ask_submit`의 `from` 값으로 갈립니다.

### ② 공유 버튼 — `ShareButtons`

지금까지 이 사이트에는 **공유 수단이 하나도 없었습니다.** 주소창을 긁는 수밖에 없었어요.

**들어간 곳 (8개)** — 위 6개 + `half-marathon-race-day`, `warmup`

세 가지로 나눴습니다.

- **링크 복사** — 키 불필요. **이미 작동합니다**
- **공유하기** — 모바일의 네이티브 공유창. 카톡·문자·메모가 다 뜹니다. 키 불필요, **이미 작동합니다**
- **카카오톡** — 키가 있어야 뜹니다. 아래 2번 참고

GA 이벤트 `share_click` (`method`: copy / native / kakao)로 어느 통로가 쓰이는지 봅니다.

---

## 2. hyun님이 해야 할 것

### ⓐ 카카오톡 공유 켜기 (선택, 10분)

한국에서 링크가 실제로 도는 통로는 카카오톡입니다. 지금은 버튼이 **안 보입니다** —
키가 없을 때 버튼을 띄우면 눌러도 아무 일이 안 일어나니까 아예 안 그리게 했습니다.

1. https://developers.kakao.com 로그인 → **내 애플리케이션** → 애플리케이션 추가
2. 앱 이름 아무거나(예: 뛰다가 아메리카노) → 저장
3. **앱 키** 메뉴에서 **JavaScript 키** 복사
4. **플랫폼 → Web** 에서 사이트 도메인 등록
   `https://ddaiga-americano.vercel.app`
   (도메인을 나중에 사면 그것도 추가해야 합니다)
5. Vercel → Settings → Environment Variables 에 추가

   ```
   NEXT_PUBLIC_KAKAO_JS_KEY = (복사한 JavaScript 키)
   ```

6. 재배포하면 카카오톡 버튼이 나타납니다

> ⚠️ **코드에 SRI(integrity) 해시를 넣지 않았습니다.**
> 처음에 넣었다가 뺐는데, 그 값이 **제가 지어낸 것**이었기 때문입니다.
> SRI는 틀리면 브라우저가 스크립트를 조용히 차단합니다 — 고장이 눈에 안 띄는 종류예요.
> 카카오 공식 문서에서 최신 버전과 해시를 확인해 `components/ShareButtons.tsx`에
> 함께 넣으면 더 안전합니다.

### ⓐ-0 ~~Google Search Console 등록~~ → **이미 돼 있었습니다** (2026-09-06 확인)

> **정정.** 이 문서에 "GSC 미등록"이라고 적어두고 절차서까지 썼는데, **열어보니 이미
> 등록돼 있었습니다.** 2026-06-21에 사이트맵까지 제출된 상태였어요.
> 확인하지 않고 "안 돼 있다"고 적은 것이 잘못입니다 — 없는 것을 지어낸 것과 같은 종류입니다.
>
> `app/layout.tsx`의 `GOOGLE_SITE_VERIFICATION` 환경변수 자리는 **그대로 둡니다.**
> 값이 없으면 태그가 안 붙어 부작용이 없고, 도메인을 사서 속성을 다시 만들 때 쓸 수 있습니다.

**그래서 진짜 원인이 무엇이었나 — 색인입니다.**

```
색인이 생성된 페이지        11개
색인이 생성되지 않은 페이지   33개   ← 사유 100%가 "발견됨 — 현재 색인이 생성되지 않음"
3개월 총 클릭              2회
```

robots.txt도 사이트맵도 정상입니다. **구글이 URL을 알면서 크롤을 안 하고 있습니다.**
`/courses`를 URL 검사에 넣으니 **"참조 페이지: 감지된 페이지 없음"** — 내부 링크가
구글에게 하나도 안 보인다는 뜻이고, 같은 날 SEO 감사가 짚은 **compare 고아 18개**와 같은 병입니다.

**2026-09-06에 한 것**

- ✅ 사이트맵 재제출 (마지막 읽음이 8/27이라 그 뒤 변경분을 구글이 몰랐습니다)
- ✅ `/courses` 색인 생성 요청 → 우선순위 크롤링 대기열
- ⚠️ `/shoe-finder` 색인 요청은 **일일 할당량 초과**로 실패

**hyun님이 내일 하실 것 (5분)** — 할당량이 초기화됩니다.

1. Search Console → 상단 검색창에 URL 붙여넣기 → Enter
2. **색인 생성 요청** 클릭
3. 아래 순서로 하루 몇 개씩

```
https://ddaiga-americano.vercel.app/shoe-finder
https://ddaiga-americano.vercel.app/tools/pace
https://ddaiga-americano.vercel.app/tools/shoe-life
https://ddaiga-americano.vercel.app/compare/hoka-clifton-10-vs-brooks-ghost-17
https://ddaiga-americano.vercel.app/injury/midfoot
```

판정은 `유입_설정_기준선.md` 4-2절에 있습니다 — **2주 뒤 색인 페이지가 11 → 20 이상**이면
내부 링크 처방이 먹힌 것이고, 안 늘면 백링크 쪽을 봐야 합니다.

### ⓑ 질문 알림 받기 (중요, 20분)

**지금은 질문이 들어와도 hyun님이 모릅니다.** 이메일·웹훅 어느 것도 없어서
Supabase 테이블을 직접 열어봐야 알 수 있습니다.

이게 커뮤니티에서 가장 큰 구멍입니다 — **답이 안 달리는 게 보이면 다음 사람도 안 씁니다.**

가장 간단한 방법은 **Supabase Database Webhook**입니다. `community_posts`에 행이
INSERT 되는 순간 발동하고, 폴링이나 크론이 필요 없습니다.

1. Supabase 대시보드 → **Database → Webhooks** → Enable
2. **Create a new hook**
   - Table: `community_posts`
   - Events: **Insert** 만 체크
   - Type: **HTTP Request**
3. 받을 곳을 정합니다. 둘 중 하나입니다.

   **간단한 쪽 — 이메일**
   Resend(https://resend.com) 가입 → API 키 발급 → Supabase Edge Function에서
   그 키로 메일 발송. Supabase 공식 문서가 Resend를 예시로 씁니다.
   **무료 한도는 제가 확인하지 못했습니다** — 가입 화면에서 직접 보세요.

   **더 간단한 쪽 — 이미 쓰는 메신저로**
   슬랙/디스코드를 쓰신다면 Incoming Webhook URL 하나만 넣으면 끝입니다.
   Edge Function도 필요 없습니다.

4. 테스트: `/injury/knee-pain`에서 직접 질문을 하나 올려보고 알림이 오는지 확인

### ⓒ 답변 화면 (아직 없음)

`community_posts.answer` 컬럼은 있는데 **답을 다는 화면이 없습니다.**
지금은 Supabase 테이블 에디터에서 `answer`에 직접 입력해야 합니다.

질문이 실제로 들어오기 시작하면 그때 만드는 게 맞습니다.
**들어오지도 않는데 관리 화면부터 만들면 또 안 쓰이는 것이 하나 늘어납니다.**

---

## 3. 구매 링크 — 17개가 죽어 있었습니다 (고쳤습니다)

> **정정, 그리고 그 정정의 정정 (2026-09-06).**
>
> · 처음 판: *"SearchFit SEO 스킬을 설치했다"* → **틀렸습니다.** 제안만 했고 설치는 안 됐습니다.
> · 두 번째 판: *"스킬 목록도 제가 지어낸 것이라 지웠습니다"* → **이것도 틀렸습니다.**
>   플러그인 레지스트리에서 실제로 찾아보니 `searchfit-seo`는 **실재하는 무료 플러그인**이고,
>   제가 적었던 `broken-links`·`technical-seo`·`content-brief`·`content-strategy`·`ai-visibility`가
>   **전부 실제 스킬 이름**이었습니다(총 11개짜리 툴킷).
> · **새 결론**: 틀린 건 "설치했다" 하나뿐이었습니다. 확인 없이 **과하게 정정한 것도
>   같은 종류의 잘못**입니다 — 둘 다 사실을 안 보고 쓴 것이니까요.
>
> 어느 쪽이든 **저장소 안 검사기를 만든 판단은 유효합니다.** 한 번 쓰고 마는 스킬보다
> 커밋마다 도는 검사기가 낫습니다. 둘은 겹치지 않고 보완합니다.

```bash
npm run check:links:plan   # 네트워크 없이 — 형태가 틀린 링크 + 쇼핑몰 대장
npm run check:links        # 실제 상태코드 조회
```

**찾은 것.** 구매 링크 406개 중 **17개가 단종 여부와 무관하게 죽어 있었습니다.**
주소 형식 자체가 틀렸던 거예요.

| 쇼핑몰 | 우리가 걸어둔 주소 | 실제로 눌러보면 |
|---|---|---|
| 플릿러너 | `/search?keyword=` | 404 "요청하신 페이지를 찾을 수 없습니다" |
| 29CM | `/search?keyword=` | "페이지가 삭제되었거나 변경됐어요" |
| 미즈노 | `www.mizuno.com/ko-kr/…` | **일본 기업 사이트**로 튕김 |

전부 고쳤고, 29CM에서 영문 검색어가 0건이라 한글로 바꿨습니다.
킨바라·엔돌핀은 29CM이 아예 취급을 안 해서 **링크를 내렸습니다** —
눌러서 빈 화면이 뜨는 것보다 없는 게 낫습니다.

**쇼핑몰별 성질** (9곳을 브라우저로 직접 눌러봤습니다)

| 성질 | 쇼핑몰 | 뜻 |
|---|---|---|
| 잘 나옴 | 다나와·크림·브룩스·아디다스·미즈노 | 단종품이 **오히려 할인가로** 남아 있음 |
| 다른 게 나옴 | 나이키 | "레볼루션 7" 검색 → 결과 4건이 **전부 레볼루션 8** |
| 일부 0건 | 플릿러너·29CM | 재고가 빠지면 빈 결과. 세대번호를 뺐음 |
| 못 봄 | 쿠팡 | 안전 정책상 열 수 없었습니다 (30개 링크, **미확인**) |

**처음 세운 가설은 틀렸습니다.** "단종되면 빈 결과가 뜰 것"이라고 봤는데,
9곳 중 0건은 두 곳뿐이었고 나머지는 잘 남아 있었습니다.
대신 **가설과 상관없는 곳에서 17개가 죽어 있었어요.**

---

## 4. 2주 뒤(9/20경) 판정할 것

새로 만든 것이 쓰이는지 GA에서 확인합니다. **0건이면 없애는 게 맞습니다.**

| 이벤트 | 뜻 | 0이면 |
|---|---|---|
| `inline_ask_submit` | 글 안에서 질문함 | 질문창 위치·문구를 바꾸거나 접는다 |
| `share_click` | 공유 버튼 누름 | 공유 버튼을 접는다 |
| `finder_cta_click` | 글 → 신발 찾기 | 9/5 기준 이미 0건. 계속 0이면 CTA 재설계 |
| `shoe_life_calc`·`pace_calc` | 계산기 사용 | 계산기 전략 자체를 접는다 |
| `injury_filter` | 부상 목록 필터 | 필터를 없앤다 |
| `home_strip_tab` | 홈 신발 띠에서 종류 고름 | 탭을 없애고 띠만 남긴다 |
| `home_shoe_click` | 홈 신발 띠에서 신발 누름 | **띠 자체를 접는다** |
| `home_strip_arrow` | 홈 신발 띠 화살표 | 화살표를 없앤다(스와이프만) |
| `/courses` 조회수 | 한강 코스 페이지 | 네이버 유입이 안 붙으면 코스를 늘리지 않는다 |

**`home_shoe_click`이 가장 중요합니다.** `finder_cta_click`이 0건이라는 건
홈의 버튼 하나가 안 눌린다는 뜻이었습니다. 신발 띠는 그 문제를 정면으로 친 것이라,
**이것마저 0이면 문제는 CTA가 아니라 홈에 오는 사람이 애초에 신발을 찾으러
온 게 아니라는 뜻**입니다. 그러면 홈이 아니라 유입 경로를 봐야 합니다.

이건 계산기 3호를 보류할 때 세운 기준과 같습니다 —
**쓰이는지 재지 않고 만든 것은 늘리지 않는다.**

---

## 4-1. 붙인 도구 — Scrapling (2026-09-06)

hyun님이 올려주신 `Scrapling.zip`을 스킬로 등록했습니다. 이름은 **`scrapling-official`**.

저장소 안에 **작성자가 직접 만든 공식 에이전트 스킬**(`agent-skill/Scrapling-Skill/`, v0.4.15)이
들어 있어서 제가 새로 쓰지 않고 그걸 등록했습니다. 제 기억으로 API를 적었으면
또 지어냈을 겁니다 — 이 저장소가 여러 번 겪은 실패입니다.

**무엇을 풀어주나.** 이번 주에 제가 막혔던 것들이 전부 이 도구의 영역입니다.

| 막혔던 것 | Scrapling으로 |
|---|---|
| 아식스 한국몰 이미지가 **클라이언트 렌더링**이라 수집 불가 | `fetch`(브라우저)로 렌더 후 추출 |
| 29CM·쿠팡 검색 결과 확인 불가 | `stealthy-fetch` |
| **쿠팡 30개 링크 미확인** (안전 정책상 못 엶) | 로컬에서 확인 가능 |
| **신제품 출시를 자동 감지 못 함** (`check:shoes`가 포기 선언한 부분) | 스파이더로 브랜드몰 크롤 |

> ⚠️ **제 작업 환경에서는 못 돌립니다.** 설치까지는 됐는데(`scrapling 0.4.15`),
> 실제 요청이 전부 `curl: (7) CONNECT tunnel failed, response 403`으로 막힙니다.
> 샌드박스 프록시가 외부 사이트를 차단하고 있어요 — `check:images`가 51장 전부
> `fetch failed`였던 것과 같은 원인입니다.
> **그래서 이건 hyun님 PC에서 돌려야 실효가 있습니다.**

**hyun님 PC에서 첫 시도 (10분)**

```powershell
pip install "scrapling[all]>=0.4.15"
```

> ⚠️ **설치는 되는데 `scrapling` 명령이 안 잡힙니다.** 2026-09-06에 실제로 겪었습니다 —
> `The term 'scrapling' is not recognized...`. 고장이 아니라 **`scrapling.exe`가
> PATH에 없는 것**이고, pip이 설치 중에 그 경고를 여러 번 찍습니다(스크롤에 묻혀 지나가기 쉽습니다).
> 공식 스킬도 *"scrapling 바이너리 경로를 기억해 두고 그걸로 실행하라"*고 적어 뒀습니다.

```powershell
$scrapling = "C:\Users\user\AppData\Local\Python\pythoncore-3.14-64\Scripts\scrapling.exe"

# 브라우저 엔진 내려받기 — playwright + patchright 두 벌이라 수백 MB, 몇 분 걸립니다
& $scrapling install --force

# 미확인으로 남은 쿠팡 하나 확인하기
& $scrapling extract fetch "https://www.coupang.com/np/search?q=Hoka%20Clifton%2010" coupang.md --ai-targeted
```

`$scrapling` 변수는 그 터미널 창에서만 삽니다. 매번 치기 싫으면 PATH에 한 번 넣어두세요
(새 터미널부터 `scrapling`으로 바로 됩니다).

```powershell
[Environment]::SetEnvironmentVariable(
  "Path",
  [Environment]::GetEnvironmentVariable("Path","User") + ";C:\Users\user\AppData\Local\Python\pythoncore-3.14-64\Scripts",
  "User")
```

`--ai-targeted`는 **반드시** 붙이세요. 프롬프트 인젝션 방어이고, 브라우저 명령에서는
광고 차단까지 같이 켜집니다. 결과를 알려주시면 `check-links.mjs`의 `VERIFIED` 대장에
쿠팡 성질을 적어 미확인 항목을 없애겠습니다.

---

## 5. 아직 안 한 것

- **쿠팡 링크 30개 미확인** — 안전 정책상 브라우저로 열 수 없었습니다. hyun님이 한 번 눌러보시면
  `check-links.mjs`의 `VERIFIED`에 성질을 적어두겠습니다.
- **쿠팡 파트너스 가입** — 승인까지 1~3일. 승인되면 `lib/shoes/affiliate.ts` 표에
  링크를 붙이기만 하면 되고, **공정위 고지 배너는 자동으로 켜집니다**(따로 켤 스위치가
  없습니다 — 링크를 넣고 고지를 잊는 일이 생기지 않게 일부러 그렇게 만들어 뒀습니다).
  API는 "최종 승인"(누적 판매 15만 원) 이후에 열리므로, **일반 링크로 먼저 시작**해야 합니다.
- **커뮤니티 씨앗** — 빈 게시판에는 아무도 안 씁니다. 다만 **가짜 질문은 절대 안 됩니다** —
  2026-08에 하드코딩된 가짜 Q&A 5건이 백엔드 사망을 두 달간 가렸습니다.
  블로그 댓글로 실제 받은 질문을 옮기거나, hyun님이 초보 때 궁금했던 걸
  본인 이름으로 올리고 답하는 건 괜찮습니다. **없는 사람을 지어내지 않으면 됩니다.**

---

## 6. 열려 있는 버그 — 하이드레이션 불일치 (2026-09-07)

**증상은 안 보입니다. 그런데 오늘 지도가 안 뜬 원인일 가능성이 큽니다.**

배포된 사이트의 **모든 페이지**에서 브라우저 콘솔에 이 오류가 한 번씩 뜹니다:

```
Minified React error #418  (하이드레이션 실패 — 서버가 만든 HTML과 클라이언트가 다름)
```

`/`, `/tools`, `/courses` 세 곳에서 각각 확인했습니다. 특정 페이지가 아니라
**`app/layout.tsx` 쪽 문제**라는 뜻입니다.

무슨 일이 벌어지나 — React가 서버 HTML을 **버리고 브라우저에서 처음부터 다시
그립니다.** 화면은 결국 정상으로 보이지만 첫 표시가 느려지고, 그 과정에서
**DOM에 꽂아둔 것이 통째로 날아갑니다.** 오늘 지도가 그렇게 사라졌습니다 —
leaflet.min.js 요청은 200으로 성공했는데 `<script>` 태그가 실행 전에 없어졌습니다.

지도 쪽은 CDN을 버리고 npm 의존성으로 바꿔서 이 영향에서 빼냈습니다.
하지만 **원인 자체는 그대로 남아 있습니다.**

### 원인을 찾는 방법 — 10초면 됩니다 (제가 못 하는 일입니다)

제 환경에서는 개발 서버와 브라우저를 동시에 붙일 수 없어서 어느 요소인지
특정하지 못했습니다. hyun님 PC에서는 바로 보입니다.

```powershell
cd C:\planing\ddaiga-americano
npm run dev
```

브라우저로 `http://localhost:3000/tools` 를 엽니다.
Next 개발 오버레이가 **서버 HTML과 클라이언트 HTML을 나란히 놓고 어디가
다른지 표시**합니다. 그 화면을 캡처해서 주시면 제가 고치겠습니다.

> 운영 배포에서는 오류 번호만 나오고, 개발 모드에서만 전문이 나옵니다.
> 그래서 이 한 단계가 필요합니다.
