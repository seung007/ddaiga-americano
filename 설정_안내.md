# 설정 안내 — 2026-09-06

오늘 붙인 것들과, **hyun님이 직접 해야 하는 설정**을 정리합니다.
코드는 다 들어갔지만 **키와 알림은 사람이 발급/설정해야** 켜집니다.

---

## 1. 오늘 붙인 것

### ① 글 안에서 바로 질문하기 — `InlineAsk`

**왜 만들었나.** `/community`는 두 달 넘게 질문이 **0건**이었습니다.
페이지가 고장난 게 아닙니다 — 라이브에서 확인했고 정상 작동합니다.
문제는 **거리**였어요. 네이버 검색으로 오는 46명은 `/injury/*`에 도착하는데,
거기서 Q&A까지 가려면 **클릭 한 번**이 필요합니다. 그 한 번에서 대부분을 잃습니다.

그래서 질문창을 **사람이 이미 서 있는 자리로** 옮겼습니다. 클릭 0회로 질문됩니다.

**들어간 곳 (6개)**

| 페이지 | 저장되는 태그 |
|---|---|
| `/injury/first-10k` | 기타 |
| `/injury/wide-foot` | 발볼 |
| `/injury/cooldown` | 기타 |
| `/injury/achilles` | 아킬레스 |
| `/injury/knee-pain` | 무릎 |
| `/injury/plantar-fasciitis` | 족저근막 |

기존 `community_posts` 테이블을 그대로 씁니다. **새 테이블도 마이그레이션도 없습니다.**
`/community` 페이지도 그대로 살아 있고, 여기서 올린 질문이 거기에도 뜹니다.

**어느 글이 질문을 만드는지**는 GA 이벤트 `inline_ask_submit`의 `from` 값으로 갈립니다.

### ② 공유 버튼 — `ShareButtons`

지금까지 이 사이트에는 **공유 수단이 하나도 없었습니다.** 주소창을 긁는 수밖에 없었어요.

**들어간 곳 (8개)** — 위 6개 + `half-marathon-race-day`, `warmup`

세 가지로 나눴습니다.

- **링크 복사** — 키 불필요. **이미 작동합니다**
- **공유하기** — 모바일의 네이티브 공유창. 카톡·문자·메모가 다 뜹니다. 키 불필요, **이미 작동합니다**
- **카카오톡** — 키가 있어야 뜹니다. 아래 2번 참고

GA 이벤트 `share_click` (`method`: copy / native / kakao)로 어느 통로가 쓰이는지 봅니다.

---

## 2. hyun님이 해야 할 것

### ⓐ 카카오톡 공유 켜기 (선택, 10분)

한국에서 링크가 실제로 도는 통로는 카카오톡입니다. 지금은 버튼이 **안 보입니다** —
키가 없을 때 버튼을 띄우면 눌러도 아무 일이 안 일어나니까 아예 안 그리게 했습니다.

1. https://developers.kakao.com 로그인 → **내 애플리케이션** → 애플리케이션 추가
2. 앱 이름 아무거나(예: 뛰다가 아메리카노) → 저장
3. **앱 키** 메뉴에서 **JavaScript 키** 복사
4. **플랫폼 → Web** 에서 사이트 도메인 등록
   `https://ddaiga-americano.vercel.app`
   (도메인을 나중에 사면 그것도 추가해야 합니다)
5. Vercel → Settings → Environment Variables 에 추가

   ```
   NEXT_PUBLIC_KAKAO_JS_KEY = (복사한 JavaScript 키)
   ```

6. 재배포하면 카카오톡 버튼이 나타납니다

> ⚠️ **코드에 SRI(integrity) 해시를 넣지 않았습니다.**
> 처음에 넣었다가 뺐는데, 그 값이 **제가 지어낸 것**이었기 때문입니다.
> SRI는 틀리면 브라우저가 스크립트를 조용히 차단합니다 — 고장이 눈에 안 띄는 종류예요.
> 카카오 공식 문서에서 최신 버전과 해시를 확인해 `components/ShareButtons.tsx`에
> 함께 넣으면 더 안전합니다.

### ⓑ 질문 알림 받기 (중요, 20분)

**지금은 질문이 들어와도 hyun님이 모릅니다.** 이메일·웹훅 어느 것도 없어서
Supabase 테이블을 직접 열어봐야 알 수 있습니다.

이게 커뮤니티에서 가장 큰 구멍입니다 — **답이 안 달리는 게 보이면 다음 사람도 안 씁니다.**

가장 간단한 방법은 **Supabase Database Webhook**입니다. `community_posts`에 행이
INSERT 되는 순간 발동하고, 폴링이나 크론이 필요 없습니다.

1. Supabase 대시보드 → **Database → Webhooks** → Enable
2. **Create a new hook**
   - Table: `community_posts`
   - Events: **Insert** 만 체크
   - Type: **HTTP Request**
3. 받을 곳을 정합니다. 둘 중 하나입니다.

   **간단한 쪽 — 이메일**
   Resend(https://resend.com) 가입 → API 키 발급 → Supabase Edge Function에서
   그 키로 메일 발송. Supabase 공식 문서가 Resend를 예시로 씁니다.
   **무료 한도는 제가 확인하지 못했습니다** — 가입 화면에서 직접 보세요.

   **더 간단한 쪽 — 이미 쓰는 메신저로**
   슬랙/디스코드를 쓰신다면 Incoming Webhook URL 하나만 넣으면 끝입니다.
   Edge Function도 필요 없습니다.

4. 테스트: `/injury/knee-pain`에서 직접 질문을 하나 올려보고 알림이 오는지 확인

### ⓒ 답변 화면 (아직 없음)

`community_posts.answer` 컬럼은 있는데 **답을 다는 화면이 없습니다.**
지금은 Supabase 테이블 에디터에서 `answer`에 직접 입력해야 합니다.

질문이 실제로 들어오기 시작하면 그때 만드는 게 맞습니다.
**들어오지도 않는데 관리 화면부터 만들면 또 안 쓰이는 것이 하나 늘어납니다.**

---

## 3. 설치한 스킬 쓰는 법

**SearchFit SEO** — 채팅창에 이렇게 치면 됩니다.

| 하고 싶은 것 | 이렇게 치세요 |
|---|---|
| 죽은 링크 찾기 | `/searchfit-seo:broken-links` |
| 색인·크롤 점검 | `/searchfit-seo:technical-seo` |
| 새 글 기획 | `/searchfit-seo:content-brief` |
| 다음에 쓸 주제 정하기 | `/searchfit-seo:content-strategy` |
| AI 검색 노출 확인 | `/searchfit-seo:ai-visibility` |

**가장 먼저 쓸 것은 `broken-links`입니다.**
`AGENTS.md`에 *"구매 링크가 실제로 결과를 반환하는지"*가 **검사기 사각지대**로 적혀 있고,
지금 구매 링크가 **406개**(그중 검색쿼리형 350개)입니다.
단종된 27종은 클릭하면 404가 아니라 **빈 검색 결과**가 뜹니다 — 조용히 실패하는 종류예요.

---

## 4. 2주 뒤(9/20경) 판정할 것

새로 만든 것이 쓰이는지 GA에서 확인합니다. **0건이면 없애는 게 맞습니다.**

| 이벤트 | 뜻 | 0이면 |
|---|---|---|
| `inline_ask_submit` | 글 안에서 질문함 | 질문창 위치·문구를 바꾸거나 접는다 |
| `share_click` | 공유 버튼 누름 | 공유 버튼을 접는다 |
| `finder_cta_click` | 글 → 신발 찾기 | 9/5 기준 이미 0건. 계속 0이면 CTA 재설계 |
| `shoe_life_calc`·`pace_calc` | 계산기 사용 | 계산기 전략 자체를 접는다 |
| `injury_filter` | 부상 목록 필터 | 필터를 없앤다 |

이건 계산기 3호를 보류할 때 세운 기준과 같습니다 —
**쓰이는지 재지 않고 만든 것은 늘리지 않는다.**

---

## 5. 아직 안 한 것

- **러닝 코스 추천 페이지** — 지도가 필요한데, 카카오맵이 유일하게 현실적입니다
  (무료·심사 없음, 2026-07-21에 **도보 경로 API** 추가됨).
  네이버 지도는 무료 한도가 없고, OSM+Leaflet은 프로덕션에서 타일 서버를 직접 띄워야 하며
  한국 데이터가 부실합니다. 지도 없이 SVG 도식으로 가는 선택지도 있습니다.
- **커뮤니티 씨앗** — 빈 게시판에는 아무도 안 씁니다. 다만 **가짜 질문은 절대 안 됩니다** —
  2026-08에 하드코딩된 가짜 Q&A 5건이 백엔드 사망을 두 달간 가렸습니다.
  블로그 댓글로 실제 받은 질문을 옮기거나, hyun님이 초보 때 궁금했던 걸
  본인 이름으로 올리고 답하는 건 괜찮습니다. **없는 사람을 지어내지 않으면 됩니다.**
