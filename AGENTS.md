<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

---

# 토큰 규약 — 적게 쓰고 크게 남긴다

**이 파일은 매 세션 통째로 주입된다.** 그래서 여기 적힌 것은 자동으로 적용된다 —
사람이 매번 "토큰 아껴"라고 말할 필요가 없어야 한다. 아래는 **실제로 낭비한 것**에서만 뽑았다.

1. **출력은 받기 전에 줄여라.** 받고 나서 줄이는 건 이미 늦었다.
   2026-09-06에 `search_plugins` 결과가 **90,886자**로 와서 파일로 떨어졌고, 다시 파싱해야 했다.
   같은 날 `web_fetch`가 한강공원 페이지를 **57,680자**로 가져와 통째로 실패했다.
   → 도구는 `--plan`·`grep -c`·`head`·`jq`·`--css-selector`로 **좁혀서 부른다.**

2. **검사는 `npm run check` 하나로.** 개별 검사기를 여섯 번 돌리지 마라.
   통과한 검사기의 출력은 읽을 이유가 없다.

3. **파일은 범위로 읽어라.** `grep -n`으로 줄번호를 먼저 잡고 그 근처만 읽는다.
   통째로 읽는 것은 200줄 미만일 때만.

4. **브라우저는 `get_page_text`·`read_page`가 기본.** 스크린샷은
   **보이는 것 자체가 판정 대상일 때만** 쓴다(레이아웃이 깨졌는가, 사진이 그 신발인가).
   글자를 읽으려고 스크린샷을 찍지 마라.

5. **같은 사실을 두 문서에 적지 마라.** 한 곳에 적고 나머지는 가리킨다.
   지금 `AGENTS.md`·`설정_안내.md`·`스킬_안내.md`가 757줄이고 겹치는 대목이 있다.
   새 문서를 만들기 전에 **기존 문서에 절을 더할 수 있는지 먼저 본다.**

6. **보고는 결론부터 한 줄.** 과정은 사용자가 이미 봤다. 도구 호출 사이에 중계하지 마라.

7. **모델을 루프에서 빼는 것이 최고의 절약이다.** 스크립트가 판정하고 모델은 exit code만 본다.
   검사기 7개·커밋 훅·주간 CI가 그 형태다 — 전부 토큰 0으로 돈다.

> ⚠️ **이 파일 자체가 고정비다.** 매 세션 통째로 들어간다.
> 사례를 더 적을 때는 **규칙이 늘었는지 사례만 늘었는지** 보고, 사례만 늘었으면
> 별도 파일로 뺄 때가 된 것이다.

## 왕복을 줄여라 — 토큰의 대부분은 생각이 아니라 왕복에서 나간다

2026-09-08 에 이 세션을 되짚어 세어 본 결과다. 낭비는 "생각을 길게 해서"가
아니라 **같은 사실을 확인하려고 사람과 여러 번 주고받아서** 생겼다.

| 병목 | 실제로 쓴 비용 | 처방 |
|---|---|---|
| CRLF 경고 106줄이 붙여넣기마다 반복 | 5회 × 2~3천 토큰 | `.gitattributes` 두 줄 |
| 내가 화면을 못 본다 | 오진 1건 + 정정 커밋 1건 + 왕복 6회 | `npm run shot` |
| `&&` 체인이 빈 커밋에서 끊겨 푸시 누락 | 왕복 2회 (옛 코드를 보고 진단했다) | `npm run ship` |
| 같은 Overpass 질의 반복 | 실행 5회 중 3회가 이미 받은 것 | `.cache/osm/` |
| 실패 메시지가 진단을 안 담음 | 406·TypeError·`?? 0` 각각 왕복 1회 | 아래 규칙 |

### 규칙

1. **사람에게 시킬 명령은 한 개로 줄이고, 출력이 짧게 나오게 만들어라.**
   명령 네 개를 주면 출력 네 덩이가 대화로 들어온다. `npm run ship` 하나면
   성공은 두 줄이다. **붙여넣기 비용이 정보량에 비례해야 한다.**
2. **성공은 한 줄, 실패는 전부.** 통과한 검사를 자세히 찍을 이유가 없다.
3. **추측하지 말고 확인해서 알려라.** `ship` 은 마지막에 로컬·원격 해시를
   비교해 "일치"를 찍는다. "푸시됐다"를 믿고 다음으로 넘어가면, 옛 코드를
   보고 내리는 진단이 뒤따른다 — 이 세션에서 실제로 그랬다.
4. **내가 못 하는 일은 사람이 한 번에 하도록 도구를 만들어 줘라.**
   나는 렌더링을 못 본다(샌드박스 네트워크가 npm 말고 전부 403,
   브라우저 바이너리 다운로드도 막힘). 그래서 화면 판정을 말로 주고받는 대신
   `npm run shot` 이 PNG 를 폴더에 떨어뜨리고 **내가 그 이미지를 직접 읽는다.**
   [내 추측 → 사용자 캡처 → 내 정정] 3턴이 [shot → 판단] 1턴이 된다.
5. **같은 것을 두 번 받지 마라.** 외부 조회 결과는 디스크에 캐시한다.
   공개 API 예절 문제이기도 하다.
6. **실패 메시지에 원인을 담아라.** `e.name` 만 찍지 말고 `message` 와
   `cause.code` 를 찍는다. 상태코드는 사람이 할 일별로 갈라 안내한다
   (서버 혼잡 / 코드 문제). 이 세션에서 부실한 실패 메시지가 왕복 세 번을 만들었다.

### 지금 있는 한 방 명령

```bash
npm run ship "메시지"   # 검사 -> 커밋 -> 푸시 -> 원격 일치 확인. 성공은 두 줄
npm run shot            # 배포 화면을 shots/ 에 PNG 로. Claude 가 직접 읽는다
npm run shot:local      # localhost:3000 을 찍는다 (npm run dev 를 먼저 켠다)
                        # `npm run shot -- --local` 은 인수가 안 넘어간 적이 있다.
                        # 전용 스크립트를 쓰면 그 실패 방식이 사라진다.
npm run routes          # 코스 경로선 (캐시됨, --fresh 로 무시)
```

⚠️ **샌드박스에서 `npm i` 를 돌릴 때는 `--ignore-scripts` 를 붙여라.**
`playwright` 가 devDependency 라서 설치 후 브라우저 바이너리를 내려받는데,
샌드박스는 그 다운로드가 막혀 있어 `npm i` 전체가 실패한다.
브라우저는 사람 PC 에만 있으면 된다.

## 스킬 라우팅 — 고민하지 말고 표를 보고 골라라

스킬이 25개를 넘었다(2026-09-06, SearchFit SEO 17개 설치). **어느 것을 쓸지 매번
따져보는 것 자체가 토큰이다.** 상황 → 스킬을 한 번에 정한다. 표에 없으면 스킬 없이 한다.

| 상황 | 쓸 것 |
|---|---|
| 논문·스펙·거리처럼 **틀리면 신뢰가 무너지는 것**을 쓸 때 | `grounded` |
| 웹에서 데이터를 긁어야 할 때 · `web_fetch` 실패 · 봇 차단 | `scrapling-official` |
| 큰 파일·긴 출력·과설계 위험 | `lean` |
| 구글 색인·크롤·속도 문제 | `searchfit-seo:technical-seo` |
| 새 페이지가 색인될 길이 있는지 | `searchfit-seo:internal-linking` |
| 어느 검색어를 노릴지 | `searchfit-seo:keyword-clustering` |
| 특정 페이지 제목·메타 최적화 | `searchfit-seo:on-page-seo` |
| 구조화 데이터(JSON-LD) | `searchfit-seo:schema-markup` |
| 되돌리기 비싼 결정 | `council` **(토큰 많이 — 아껴 쓴다)** |
| 우선순위가 헷갈릴 때 | `ai-saas-builder` |

**구매 링크·사진·인용·영상 검사는 스킬이 아니라 `npm run check`다.** 저장소 검사기가
더 정밀하고 토큰이 0이다. `searchfit-seo:broken-links`와 겹치면 **우리 것을 쓴다.**

### 대량 탐색은 서브에이전트로 넘겨라 — 이게 가장 큰 절약이다

`seo-auditor`·`content-strategist`·`competitor-analyzer`는 **별도 문맥에서 돈다.**
사이트를 크롤하고 파일을 훑는 과정이 이쪽 대화에 안 쌓이고 **결론만 돌아온다.**

그러므로 **"여러 파일·여러 페이지를 훑어야 답이 나오는 일"은 직접 하지 말고 넘긴다.**
직접 하면 훑은 내용이 전부 문맥에 남지만, 넘기면 요약 한 덩어리로 끝난다.
반대로 **파일 한두 개를 고치는 일은 넘기지 마라** — 넘기는 비용이 더 크다.

---

# 출처 규칙 — 짐작으로 쓰지 않는다

이 사이트의 **유일한 차별 주장**은 "광고 아닌, 근거를 밝히는 추천"이다.
그 주장이 무너지면 남는 게 없다. 그런데 실제로 이런 일이 있었다.

| 점검 | 검사한 것 | 틀린 것 |
|---|---|---|
| 논문 인용 (2026-08-27~31) | 33건 | **26건** |
| 유튜브 채널명 (2026-08-31) | 48건 | **40건 (전부)** |
| 신발 단종 표기 (2026-08-31) | 52종 | **23종 미표기** |

전부 **사람이 눈으로 세 번 이상 훑은 뒤**에 나온 숫자다.
눈으로 확인하는 방식은 이 저장소에서 이미 실패했다.

## 1. 확인 없이는 구체적인 것을 쓰지 않는다

아래는 **모두 실제로 지어낸 적이 있는 항목**이다. 확인 전에는 쓰지 마라.

- **유튜브 채널명** — "재활운동TV", "러닝코치TV", "달리기연구소", "정형외과TV", "건강TV"
  전부 지어낸 이름이었다. 실제 채널은 "달려라하나", "알쓸물치", "통증요정 김학조"처럼 개성이 있다.
  **작화의 신호는 "그럴듯하게 일반적인 이름"이다.**
- **논문 저자명** — "Beyer et al. (2023)"의 제1저자는 Kim이었다
- **PMID / DOI** — 러닝 페이지에 뇌진탕·상완골 골절·피부암·노닐페놀 유전자·대뇌피질 논문이 걸려 있었다
- **논문 속 수치** — "HRV 5ms 하락 시 VO₂max 2배"는 그 논문에 없는 숫자였다
- **영상·논문 제목의 구체성** — "장경인대 스트레칭 **30초 핵심**"에서 "30초 핵심"은 없는 말이었다
- **서지 정보** — 권·호·페이지를 기억으로 채우지 마라

## 2. 검사기를 돌린다

```bash
npm run check:citations       # 논문: 저자·연도가 링크된 실제 논문과 맞는지
npm run check:youtube         # 영상: 살아 있는지, 채널명이 실제 업로더와 맞는지
npm run check:youtube:todo    # 아직 검색 URL로 남은 자리 (오프라인)
npm run check:shoes           # 신발: 어느 모델을 언제 확인했는지 (원장 lib/shoes/verified.json)
npm run check:figures         # 동작 그림: 관절 각도를 계산해 발이 바닥을 뚫는지
npm run check:links           # 구매 링크 406개: 상태코드
npm run check:links:plan      # 확인할 쇼핑몰 큐 (오프라인)
npm run check:images          # 신발 사진 52장: 실제로 뜨는지 + 출처가 어디인지
npm run check:images:plan     # 출처 분포만 (오프라인)
npm run check:internal        # 내부 링크: 고아 페이지·끊긴 경로·sitemap 누락 (오프라인)
npm run check:cdn             # 손으로 꽂는 외부 CDN 스크립트 금지 (오프라인)
npm run check:routes          # 코스 경로선이 말이 되는 값인지 (오프라인)
npm run routes                # 코스 경로선 받아오기 — 네트워크 되는 곳에서만
```

**하나씩 돌리지 마라. 묶음이 있다.**

```bash
npm run check        # 오프라인 검사 10개 — 통과한 것은 ✓ 한 글자, 실패한 것만 전체 출력
npm run check:all    # 네트워크 실조회까지 전부
npm run hooks:on     # 커밋 훅 켜기 (저장소당 한 번)
```

`check-all.mjs`가 원칙을 뒤집었다 — **통과한 검사기의 출력은 읽을 이유가 없다.**
여섯 번 돌려 여섯 번의 출력을 읽던 것이 한 화면이 됐다. 사람에게는 번거로움이고
에이전트에게는 토큰이다. 조용히 지나가는 것이 위험하지, 조용히 통과하는 것은 위험하지 않다.

**네트워크 조회는 이 환경에서 의미가 없다.** 샌드박스 프록시가 외부를 막는다 —
`check:images` 51장이 전부 `fetch failed`였고 Scrapling도 `curl (7) CONNECT tunnel failed 403`이었다.
그래서 진짜 조회는 **GitHub Actions가 매주 월요일에 대신 한다**(`.github/workflows/checks.yml`).
우리가 안 건드려도 상대가 없앤다 — 이번 주에 브랜드 공식몰 3곳이 죽어 링크 14개가 같이 죽었다.

**출처를 하나라도 건드렸으면 커밋 전에 돌린다.** 셋 다 불일치·기한초과 시 exit 1이다.

`check:shoes`는 앞의 둘과 성격이 다르다. **신제품 출시를 스스로 감지하지 못한다** —
브랜드마다 공지 방식이 다르고 공개 API가 없다. 억지로 스크래핑하면 자주 깨지고,
깨진 걸 모른 채 지나가면 지금과 똑같아진다 — 조용히 틀린 데이터.
대신 **마지막 확인 날짜를 원장에 남기고 오래된 것부터 큐로 뽑아준다.**
자동화할 수 없는 것을 자동화한 척하지 않는 게 이 도구의 설계 원칙이다.

**인용 서식은 관행을 따라라 — `저자 & 저자 (연도) 저널 권(호):쪽 — 설명 ↗`, 링크 안에 한 줄.**
검사기는 요소 경계(`<`)를 넘지 않으므로 저자와 연도 사이에 `<em>제목</em>`을 끼우면
연결이 끊기고 **연도 바로 앞 토큰이 제1저자로 잡힌다.** 2026-09-02에 shoe-life 페이지가
`Int J Sports Phys Ther. 2017`의 "Int"를 저자로 읽혔다. 이번엔 불일치로 걸렸지만,
학술지 약어가 우연히 실제 제1저자의 성과 겹치면 **틀린 인용이 조용히 통과한다.**

**꼬리만 보고 "통과"라고 하지 마라.** 2026-09-02에 `check:citations`를 `| tail -4`로
돌려 끝의 안내 문구만 보고 "인용검사 통과"라고 보고했다. 실제로는 **35건 전부
`fetch failed`였고 exit 1이었다.** 요약 줄은 그 위에 있었다.
판정은 **exit code로 한다** — `cmd > /tmp/out 2>&1; echo $?`. 파이프를 걸면
`$?`는 파이프 마지막 명령(head/tail)의 것이라 항상 0이다. 이건 검사기를 돌린 게 아니라
검사기 출력의 끝부분을 본 것이고, **검사기가 없는 것과 같다.**

그리고 검사기는 **"틀렸다"와 "확인 못 했다"를 갈라서 보고해야 한다.** 둘 다 exit 1이지만
(fail closed) 뜻이 다르다 — 전자는 고칠 게 있는 것이고, 후자는 **아무것도 검증되지
않았다**는 뜻이다. 뭉뚱그리면 "어차피 네트워크 문제겠지" 하고 진짜 불일치까지 넘긴다.

**개수를 주석으로 세지 마라.** data.ts 주석에 "미확인 7종"이라고 적혀 있었는데
실제로는 8개였다 — Adrenaline GTS 25 여성 버전을 빠뜨렸다. 모델 단위로 세고
SKU 단위로 관리하면 반드시 어긋난다. 세는 일은 검사기에 맡기고 주석에는 경위만 적어라.

### 사례 — 3주를 끈 사이트 전체 버그는 `<svg><title>` 한 줄이었다 (2026-09-08)

배포된 **모든 페이지**에 React 하이드레이션 오류(#418)가 있었다. React 가
**서버 HTML 을 통째로 버리고 다시 그린다.** 그 과정에서 내가 head 에 꽂은
Leaflet 스크립트가 날아간 적도 있다(그때 나는 "지도가 고장났다"고 오진했다).

**그런데 화면은 정상으로 보인다.** 그래서 아무도 몰랐다.

원인:

```tsx
<svg role="img" aria-labelledby={`cf-${name}-t ...`}>
  <title id={`cf-${name}-t`}>{name} 구간 도식</title>   // <- 이 한 줄
```

React 19 는 `<title>` 을 **문서 메타데이터로 보고 `<head>` 로 올린다.**
SVG 안이라도 클라이언트 경로에서 그 구분을 못 한다. 서버는 SVG 안에 그리고
클라이언트는 올려버려서 트리가 어긋난다. 접근성 이름은 `aria-label` 로 주면 된다.

왜 이제야 찾았나 — **찾을 수단이 없었다.** 운영 배포는 오류 번호만 주고,
전문은 개발 모드에서만 나온다. 나는 개발 서버와 브라우저를 동시에 붙일 수 없다.
`npm run shot:local` 을 만든 뒤 **한 번에** 나왔다. 도구를 만든 것이 진단이었다.

규칙:

- **`<svg>` 안에 `<title>` 을 쓰지 마라.** `aria-label` + `<desc>` 를 쓴다.
  `npm run check:cdn` 이 이제 이것도 막는다.
- **화면이 정상으로 보이는 버그가 가장 오래 산다.** 콘솔을 자동으로 모아라.
- 그 검사를 만든 직후 **오탐 5건**이 났다 — 내가 방금 쓴 **주석 안의 `<title>`**
  을 코드로 읽었다. 규칙을 설명하는 문장이 그 규칙 위반으로 잡히는 꼴이다.
  **범위를 넓히면 오탐부터 잡아라** — 오탐이 남은 검사는 사람이 곧 무시한다.

### 사례 — 원인을 하나로 단정했다 (2026-09-08, 위 사례의 후속)

뚝섬 경로선이 한강을 가로지른 원인을 나는 **"내가 hint 좌표를 지어냈기 때문"**
이라고 단정했다. 지어낸 것은 사실이고 그건 그것대로 잘못이다. 그래서
hint 를 공식 자양역 좌표로 바꾸고 **고쳤다고 적었다.**

다시 돌리니 **완전히 같은 값이 나왔다** — 2.24km, 점 5개, 비율 1.01배.
경로는 그대로 강을 가로지른다. 즉 **hint 는 원인의 일부였을 뿐이고,**
진짜 원인은 그 bbox 안 길들의 연결 구조(강을 건너는 비-교량 연결)다.

두 가지 규칙:

1. **"고쳤다"는 다시 돌려서 값이 바뀐 것을 보고 말하라.** 원인을 하나 찾아
   고치면 그게 유일한 원인이었다고 믿기 쉽다. 같은 입력에 같은 출력이면
   아무것도 안 고친 것이다.
2. **잘못이 두 개면 하나를 고쳐도 증상은 남는다.** 지어낸 좌표(잘못 A)를
   고치는 것과 경로가 강을 건너는 것(증상 B)은 별개였다.

임시 처방: 「희박하면서 곧은 선」 검사를 넣었다(점/km < 3 이고 비율 < 1.05).
잠실 4.9점·1.12배, 반포 9.4점·1.15배는 통과하고 뚝섬 2.2점·1.01배만 떨어진다.
**이건 어림짐작이라고 코드에 적어 뒀다** — 수치로 "선이 물 위에 있다"는 확인할
수 없다. 뚝섬은 경로선 없이 두고, 화면으로 확인될 때까지 그대로 둔다.

### 사례 — 숫자는 전부 통과했는데 선이 한강을 가로질렀다 (2026-09-08)

`npm run shot` 을 만들어 **처음으로 배포 화면을 직접 봤다.** 뚝섬 지도에서
경로선이 **강 위를 직선으로 가로지르고 있었다.**

그런데 내 검사는 전부 통과였다:

| 지표 | 값 | 판정 |
|---|---|---|
| 길이 / 양끝 직선 | 1.01배 | 통과 (1.0 이상) |
| 점 개수 | 5개 | 통과 (4개 이상) |
| 끝점 보정 | 334 / 219m | 통과 (700m 이하) |

**전부 정상 범위다.** 수치로는 잡을 수 없었다.

원인은 더 나빴다. 뚝섬에는 공식 자료에서 확인한 공원 진입 지점이 없었다.
그래서 나는 스크립트에 이렇게 적었다:

> *"두 끝점의 중간을 **북쪽으로 400m 옮긴 점**을 쓴다 — 뚝섬은 북쪽 강변이다."*

**좌표를 지어낸 것이다.** §1 이 금지하는 바로 그것이고, 주석에 근거처럼 적어 둬서
더 그럴듯해 보였다. 그 점이 강 위/엉뚱한 길에 붙어 경로가 그쪽으로 갔다.

세 가지 규칙:

1. **입력을 지어내면 검사가 무의미하다.** 검사는 출력이 말이 되는지 보는데,
   입력이 가짜면 "말이 되는 가짜"가 통과한다. 지어낸 값에는 검사를 붙일 수 없다.
2. **파생할 수 있는 값을 손으로 적지 마라.** 지금 이 스크립트는 좌표를
   `lib/courses.ts`(출처·확인일이 붙은 원장)에서 읽는다. 진입 지점이 없는 코스는
   **건너뛴다** — 지도는 그 코스만 경로선 없이 그려진다.
3. **화면으로만 잡히는 결함이 있다.** 수치 검사를 아무리 늘려도 "선이 물 위에
   있다"는 못 잡는다. 그래서 `npm run shot` 이 검사기 목록만큼 중요하다.
   내가 화면을 못 보는 동안 이 결함은 **배포된 채로 살아 있었다.**

같은 실행에서 화면으로만 잡힌 것 둘 더:
   · 거리 칩이 줌 버튼과 겹쳐 **"편도" 두 글자가 가려졌다.**
   · U턴 핀이 강 가운데(다리 위)에 떠서 **경로선과 이어지지 않았다.**
둘 다 코드로는 정상이고, 픽셀로는 틀렸다.

### 사례 — `?? 0` 하나가 검사를 껐다 (2026-09-08)

경로선 검사에 "끝점 보정이 700m 를 넘으면 실패"를 넣었다. 그리고 검사가
읽을 값을 원장에 남기게 했다. 그런데 **필드를 나중에 추가했다.**
그 전에 저장된 여의도 항목에는 값이 없었고, 내 코드는 이랬다:

```js
snapFromM: r.snapFromM ?? 0,
```

**0m 는 완벽한 값이다.** 그래서 보정 939m 짜리 — 선이 U턴 핀에 닿지도 않는 —
경로가 검사를 통과해서 배포됐다. 사람이 화면에서 볼 때까지 아무도 몰랐을 것이다.

세 가지가 겹쳤다:

1. **없는 값에 관대한 기본값을 줬다.** 이건 검사를 끄는 것과 같다.
   모르면 **모른다고 실패**해야 한다.
2. **원장에 없는 값은 검사할 수 없다.** 검사 항목을 늘릴 때는 그 값이 원장에
   기록되는지 같이 확인해야 한다.
3. **네트워크 실패와 판정 실패를 같게 다루지 않은 건 옳았다** — 서버가 죽었다고
   좋은 데이터를 지우면 안 된다. 그런데 그 결과로 **나쁜 옛 값이 살아남았다.**
   두 정책이 다 맞는데 조합이 틀린 경우다. 그래서 `--check` 를 별도 관문으로 둔다.

규칙: **검사에 필요한 값이 없으면 통과가 아니다.** `?? 0`, `|| ""`,
`?.length ?? 0` 같은 기본값을 검사 경로에 쓰지 마라 — 판정을 무조건 통과로
바꾸는 방향이면 특히.

### 사례 — 검사가 옳은 데이터를 버렸다 (2026-09-08)

경로선 검사에 이 규칙을 넣었다. 규칙 자체는 맞다:

> **길은 두 끝점 사이 직선거리보다 짧을 수 없다.**

그런데 **비교 대상이 틀렸다.** 나는 *다리 좌표* 사이 직선과 비교했는데,
경로가 실제로 잇는 것은 **강변에 스냅된 점**이다. 다리는 강 가운데에 있으니
스냅에서 400~600m 가 빠진다. 그래서:

| | 값 |
|---|---|
| 여의도 경로 | 1.01km |
| 다리 좌표 사이 직선 | 1.15km → **"불가능한 값"으로 판정** |
| 실제 양 끝점 사이 직선 | (스냅 기준) 1.01km 보다 작다 → **정상** |

**서로 다른 두 점 사이의 거리를 비교했으니 애초에 판정할 수 없는 값이었다.**
검사가 멀쩡한 데이터를 버렸다. 잠실도 2.49 → 2.88km 로 바뀌어 비율이
1.3배에서 1.12배가 됐다 — 원래 값이 더 그럴듯하다.

규칙:

- **검사를 만들 때도 같은 것끼리 비교하는지 먼저 확인하라.** 부등식이 참이어야
  하는 관계인지(경로 ≥ 같은 두 점 사이 직선) 따져야, 위반이 곧 오류가 된다.
- **검사가 틀리면 옳은 데이터를 버린다.** 통과 못 한 항목을 버리는 설계
  (여기서는 "쓰지 않는다")는 검사가 정확할 때만 안전하다.
- 질문을 섞지 마라. "경로 길이가 말이 되나"와 "끝점이 엉뚱한 데 붙었나"는
  다른 질문이고, 지금은 다른 지표로 본다(`ratio` / `snapFromM`).

### 사례 — 오류를 `e.name` 만 찍어서 원인을 지웠다 (2026-09-07)

406 을 고치면서 User-Agent 를 붙였다. 이렇게 썼다:

```js
const UA = "ddaiga-americano/1.0 (러닝 코스 경로선; https://...)";
```

**HTTP 헤더 값은 ASCII(ByteString)만 허용된다.** Node 는 이걸 정확히 알려준다:

```
TypeError: Cannot convert argument to a ByteString because the character
at index 22 has a value of 47084 which is greater than 255
```

그런데 내 catch 는 `e.name` 만 담았다. 사용자 화면에 남은 것은 이랬다:

```
overpass-api.de TypeError / kumi.systems TypeError / private.coffee TypeError
```

**세 서버가 전부 죽은 것처럼 보인다.** 실제로는 요청이 나가지도 않았다.
index 22 라고 정확히 짚어 주는 메시지를 내가 버렸다.

규칙:

- **오류는 `name` 이 아니라 `message` 를 찍는다.** `cause?.code`(ENOTFOUND,
  ECONNREFUSED 등)도 같이 찍는다. 이름만 남기면 진단이 사라진다.
- **헤더 값·URL·파일명에 한글을 넣지 마라.** 이 저장소는 주석과 사용자 문구를
  한글로 쓰지만, **프로토콜 값은 ASCII 다.**
- 같은 실수를 두 번 했다는 점이 중요하다. 406 사례는 "상태코드를 원인으로 번역
  하지 마라"였고, 이번은 "원인을 아예 지워 버렸다"다. 둘 다 **진단 정보를 다루는
  방식**의 문제다. 실패 경로의 출력은 성공 경로만큼 신경 써서 만들어라 —
  실패 메시지는 사람이 다음에 무엇을 할지 정하는 유일한 입력이다.

### 사례 — 406 을 "서버 혼잡"으로 읽었다 (2026-09-07)

`npm run routes` 가 사용자 PC 에서 전부 실패했다.

```
overpass-api.de 406 / kumi.systems 429 / private.coffee 429
```

내 스크립트는 이걸 **"Overpass 공개 서버 혼잡일 가능성이 큽니다 — 나중에 다시
실행하세요"** 로 안내했다. **틀렸다.** 406(Not Acceptable)은 혼잡이 아니라
**"이 요청은 받아들일 수 없다"**는 뜻이다. 원인은 서버가 아니라 내 코드였다.

빠져 있던 것 둘:
1. Overpass 문서 형식은 `data=<urlencoded>` + `application/x-www-form-urlencoded`.
   나는 질의문을 그대로 body 에 넣었다. **브라우저에서는 됐다** — 브라우저 fetch 가
   붙여 주는 `text/plain` 을 서버가 관용적으로 받아 줬기 때문이다. Node 는 안 된다.
2. **User-Agent 가 없었다.** Overpass 예절과 미러들의 차단 규칙이 요구한다.
   미러의 즉각적인 429 도 혼잡이 아니라 이것 때문일 가능성이 크다.

두 가지 규칙:

- **상태코드를 원인으로 바로 번역하지 마라.** 이 저장소에 이미 있던 교훈의 반대편이다
  (거기서는 200 을 성공으로 읽었고, 여기서는 4xx 를 남의 탓으로 읽었다).
- **잘못된 안내는 침묵보다 나쁘다.** "나중에 다시 실행하세요"는 사람을 영원히
  기다리게 한다. 지금은 상태코드별로 *서버 혼잡* 과 *질의 형식 거부(코드 문제)* 를
  나눠서 찍는다. **사람이 할 일이 다르면 문구도 달라야 한다.**
- 브라우저에서 되는 요청이 Node 에서 된다고 가정하지 마라. 헤더 기본값이 다르다.

### 사례 — 없으면 지어내지 말고 **찾아라** (2026-09-07)

코스 지도에 **경로선을 그리지 않았다.** 이유는 이렇게 적어 뒀다:

> 산책로 실제 경로 좌표를 나는 갖고 있지 않다. 다리 좌표를 이어 선을 그으면
> 강 위를 가로지르는 엉뚱한 선이 된다.

걱정은 옳았다. **결론이 틀렸다.** 사용자가 그걸 한 문장으로 짚었다 —
*"맵 보고 모르면 그냥 나가는 거야."* **경로선 없는 코스 지도는 코스 지도가 아니다.**

한강 자전거길은 **OSM 에 이미 그려져 있다.** 지도 타일에 보이는 그 선이다.
Overpass 로 받아 최단경로를 구하면 실제 선형과 실제 거리가 나온다.
"내가 가진 데이터에 없다"를 "존재하지 않는다"로 읽은 것이 잘못이다.

**작화 금지 규칙의 목적은 페이지를 비워 두는 것이 아니다.** 비우기는 확인이
불가능할 때의 최후 수단이고, 그 전에 **확인할 방법을 찾는 것**이 먼저다.
규칙을 방패로 쓰면 사용자가 쓸 수 없는 페이지가 남는다.

같이 나온 검사 규칙 하나 — **길은 직선거리보다 짧을 수 없다.**
첫 시도에서 여의도 0.91km(직선 1.1km), 잠실 2.24km(직선 2.5km)가 나왔다.
불가능한 값이다. 원인은 경로가 다리 위 자전거길로 강을 건너 질러갔고(전자),
다리를 전부 빼자 지천 교량에서 길이 끊긴 것(후자)이었다.
비율 검사 한 줄이 둘 다 잡는다. `npm run check:routes` 가 이걸 본다.
**통과 못 한 코스는 파일에 쓰지 않는다 — 틀린 선은 선이 없는 것보다 나쁘다.**

### 사례 — "지도가 안 뜬다"의 원인은 내 관측 환경이었다 (2026-09-07)

**먼저 정정.** 바로 아래 사례에서 나는 "배포된 화면에서 지도 4개 중 3개가
안 떴다"고 단정했다. **그 단정은 근거가 부족했다.**

내가 브라우저를 자동화로 몰던 탭은 `document.visibilityState === "hidden"`
이었다. 숨은 탭에서는 브라우저가 프레임을 그리지 않고, 그러면

* `requestAnimationFrame` 이 **아예 안 온다**,
* `IntersectionObserver` 콜백도 **전달되지 않는다**.

지도는 `IntersectionObserver` 로 화면에 들어올 때만 만든다. 즉 **숨은 탭에서는
설계상 절대 안 뜬다.** 사용자 화면의 고장이 아니라 관측 장치의 성질이다.
확인 방법도 이걸로 갈렸다 — 스크린샷을 찍어 강제로 렌더시키자
그 순간 지도가 생겼다.

증거가 어긋난 것도 있었다. 나는 "요청은 200인데 `window.L` 이 undefined"를
근거로 들었는데, 그 리소스 기록이 **내가 직전 호출에서 직접 실행한 `fetch()`**
와 구분되지 않는다. 같은 파일을 두 경로로 받아놓고 한쪽만 세었다.

규칙:

1. **자동화 브라우저로 본 화면을 사용자 화면이라고 부르지 마라.**
   화면을 판정하기 전에 `document.visibilityState` 와 rAF 도착을 먼저 확인한다.
   숨어 있으면 그 관측은 버린다.
2. 화면 판정은 **사람에게 확인을 받아라.** 나는 렌더링되는 창을 못 가진다.
3. **내가 만든 네트워크 요청과 페이지가 만든 요청을 섞어 세지 마라.**

아래 사례에 적은 세 가지 수정(CDN 제거·DOM 소유권 분리·타임아웃)은 그대로 둔다.
셋 다 원인 진단과 무관하게 옳은 변경이다. 다만 **"운영에서 깨져 있던 것을
고쳤다"고 말할 근거는 없다.**

### (근거 부족) 사례 — 200으로 성공한 스크립트가 실행되지 않았다 (2026-09-07)

> ⚠️ 위 정정을 먼저 읽어라. 아래 관측 상당수가 숨은 탭에서 나온 것이다.


코스 페이지 지도 4개 중 3개가 안 떴다(한 번은 4개 다). 남는 화면은
**"지도 불러오는 중…" 한 줄**이다. 실측:

| 본 것 | 값 |
|---|---|
| leaflet.min.js 네트워크 요청 | **200**, 37KB, 417ms |
| `window.L` | `undefined` |
| head 에 꽂은 `<script>` 태그 | **DOM 에 없음** |

요청은 성공했고 스크립트는 실행되지 않았다. 그러면 `onload` 도 `onerror` 도
오지 않아 Promise 가 **영원히 pending** 이다. 그래서 오류 문구조차 안 뜬다.

배경: 모든 페이지에 React 하이드레이션 불일치(#418)가 있어서 React 가 서버
HTML 을 버리고 다시 그린다. 그때 **내가 손으로 꽂은 노드가 같이 날아갔다.**

세 가지 규칙이 나왔다.

1. **외부 CDN 스크립트를 손으로 꽂지 마라.** npm 의존성 + `await import()` 를 쓴다.
   번들러가 코드 분할까지 해주고, React 와 문서를 다투지 않는다.
   `npm run check:cdn` 이 이걸 막는다.
2. **한 DOM 노드를 React 와 라이브러리가 같이 소유하게 하지 마라.**
   Leaflet 컨테이너 div 에 React 가 자식(로딩 안내문)을 렌더하고 있었다.
   안내문은 형제 오버레이로 빼야 한다.
3. **모든 로딩 상태에 타임아웃을 넣어라. 영원한 로딩 표시는 오류보다 나쁘다** —
   기다리면 될 것처럼 보이니 사람이 떠나지도 알리지도 않는다.

그리고 **"차단도 결품도 200으로 온다"에 하나 더 붙는다 — 실행 실패도 200으로 온다.**

### 사례 — "고아 페이지"라는 진단이 틀렸다 (2026-09-06)

Search Console 이 **색인 11 / 미색인 33**, 사유 100% "발견됨 — 현재 색인이
생성되지 않음"을 보여줬다. `/courses` 검사에는 **"참조 페이지: 감지된 페이지 없음"**.
서브에이전트(seo-auditor)도 "compare 22개 중 18개가 고아"라고 짚었다.
두 근거가 맞아떨어져서 나는 **"고아 페이지가 원인으로 확인됐다"**고 적었다.

그 다음 날 `check:internal` 을 만들어 실제로 링크 그래프를 세어 보니,
**고아는 0개였다.** 52개 라우트 전부에 인바운드가 있었고, sitemap 도 전부 일치했다.
compare 18개는 그 전에 이미 고쳤는데 내가 그 사실을 진단에서 빼먹었다.

무엇을 잘못했나 — **GSC 문구를 코드 사실로 번역했다.**
"참조 페이지: 감지된 페이지 없음"은 *"링크가 없다"*가 아니라
*"링크가 걸린 페이지를 구글이 아직 크롤하지 않았다"* 일 수도 있다.
색인이 11개뿐이니 후자가 오히려 자연스러웠다. 나는 두 해석 중
**내가 이미 갖고 있던 가설과 맞는 쪽만 골랐다.**

서브에이전트 결론이 겹친 것은 독립 근거가 아니었다 —
같은 저장소를 같은 시점에 본 것이라 같은 착시를 공유한다.
**두 근거가 같은 곳에서 왔으면 하나로 세라.**

규칙: 외부 도구의 문구를 원인으로 옮겨 적기 전에, **그 주장을 저장소에서
직접 셀 수 있는지 먼저 보라.** 셀 수 있으면 세고, 세었으면 검사기로 남겨라.

## 3. 검사기가 안 보는 자리에 문제가 고인다

`/shorts/` 링크는 `?v=` 패턴이 아니라 검증에서 빠져 있었다.
그리고 **하필 그 자리에 지어낸 채널명이 남아 있었다.**
검사기를 고치자마자(48 → 57개) 13개가 드러났다.

**2026-09-03: 블로그 원고가 통째로 검사망 밖에 있었다.** `SCAN_DIRS`가 `app`·`lib`·`components`뿐이라
`네이버블로그_*.md` 9편이 한 번도 검사받지 않았다. 그 사이 사이트의 인용 26건을 정정하는 동안
**원고는 정정 전 주장을 그대로 유지했고**, 네이버 유입이 76%라 **검사받지 않는 쪽이 더 많이 읽히고 있었다.**
지금은 루트의 `네이버블로그_*.md`도 스캔한다(36 → 44건).

원고를 넣자마자 두 가지가 더 드러났다. 형식이 바뀌면 검사기도 같이 바꿔야 한다는 뜻이다.

- **평문 식별자를 못 봤다.** 원고는 네이버 본문용이라 `(PMID 24923269)`처럼 맨 숫자로 적는데
  탐지 패턴이 URL만 보고 있었다. `\bPMID[:\s]+(\d{6,9})\b`와 `\b(PMC\d{6,9})\b`를 추가했다.
- **마크다운은 줄 하나가 곧 요소다.** `BOUNDARY_RE`가 `<li>`·`<p>`만 경계로 보니
  불릿 목록에서 **다음 줄 저자명을 가져왔다** — PMID 32813597(Xu 2021)이 아래 줄 "Almeida 2015"와
  짝지어졌다. 이 스크립트가 첫 실행에서 겪은 버그(앞 `<li>`의 저자명을 가져옴)의 마크다운 판본이다.

**범위를 넓히면 오탐도 같이 온다. 넓힌 직후 반드시 오탐부터 잡아라.**
평문 식별자 탐지를 켜자 두 가지가 터졌다.

- **주석 속 "틀렸다고 기록해둔 PMID"를 실제 인용으로 읽었다.** `beginner-guide:150`의
  8/28 정정 주석이 그랬다. 이대로 두면 **정직하게 기록할수록 검사기가 시끄러워진다.**
  지금은 코드 파일의 주석을 공백으로 덮고 스캔한다(오프셋 보존 → 줄번호 유지).
  단순히 `//`를 자르면 `"https://…"`가 날아가므로 따옴표 상태를 추적해야 한다.
- **같은 인용이 2번 계상됐다.** URL 패턴이 잡은 `…/articles/PMC9878810`을
  평문 패턴이 또 잡았다. 이제 **먼저 잡힌 구간과 겹치면 버린다** — URL 표기가 평문을 이긴다.

넓힌 보람은 있었다. `it-band:141`의 `PMC11377285`는 평문이라 그동안 검사망 밖이었다.

**2026-09-06: 위험을 좁히다가 더 흔한 위험을 놓쳤다.** `check:links`를 만들면서
위험 큐를 "단종 × 검색형 × 세대번호"로 좁혔다. 102개까지 줄어 잘 좁힌 줄 알았는데,
쇼핑몰을 하나씩 눌러보니 **그 세 조건 중 어느 것에도 안 걸리는 17개가 이미 죽어 있었다.**

- `fleetrunner.co.kr/search?keyword=` → 404. 실제 경로는 `/goods/goods_search.php?keyword=`
- `29cm.co.kr/search?keyword=` → 삭제된 페이지. 실제는 `/store/search?keyword=`
- `www.mizuno.com/ko-kr/search?q=` → 일본 기업사이트로 리다이렉트. 한국몰은 `kor.mizuno.com`

**경로가 틀린 링크는 단종보다 흔하고 더 치명적이다.** 단종은 다른 모델이라도 보여주지만
경로 오류는 아무것도 안 보여준다. 그런데 내 가설은 "단종"에만 걸려 있었다.
지금은 `check-links.mjs`의 `VERIFIED` 대장에 **사람이 눌러본 URL 형태**를 적어두고
거기서 벗어나면 exit 1이다. `verified.json`과 같은 발상 —
자동으로 알 수 없는 것을 사람이 한 번 확인하고, 그 확인을 날짜와 함께 기억시킨다.

그리고 **넓히자마자 오탐이 나왔다**(이번에도). 나이키 shape를 `?q=`만 적었더니
`/kr/w/pegasus-shoes-8nexhzy7ok` 카테고리 링크 4개를 깨진 것으로 잡았다.
눌러보니 정상이었고 오히려 검색보다 나은 형태였다. 형태를 넓혀 오탐을 없앴다.

새 종류의 출처를 추가하면 **검사기 범위부터 늘려라.** 아직 안 보는 것들:

- 200인데 **찾던 모델이 아닌 것이 나오는** 경우 — 자동 판정 불가. 대장에 성질을 적어 사람이 본다
- ~~아직 눌러보지 않은 쇼핑몰~~ → 13곳 전부 확인 완료 (2026-09-06). 쿠팡만 '확인불가'로 확정
- `bjsm.bmj.com/content/48/11/871`처럼 DOI·PMID가 없는 서지 URL
- **논문의 결론을 반대로 서술한 경우** — 서지는 완벽한데 주장이 반대다.
  Richards 2009는 "발 타입으로 회내 제어화를 처방할 근거가 없다"는 논문인데
  그 관행의 **지지 근거로** 인용되고 있었다. `--abstracts`로 초록을 띄워 사람이 대조해야 한다.
- **사진 속 신발이 그 모델이 맞는지** — `check:images`는 응답만 본다. 이건 사람이 봐야 한다.
  → `npm run check:images:sheet` 로 51장을 한 화면에 깔아 1분에 훑는다(2026-09-06 신설).

**2026-09-06: 차단은 200으로 온다 — 예측이 실제로 맞았다.**
`check-images.mjs` 머리말에 *"차단은 보통 200에 HTML로 온다"*고 적어 뒀는데,
쿠팡에서 그대로 재현됐다. Scrapling이 `Fetched (200)`을 찍었고 파일도 저장됐는데
내용은 **`Access Denied` 한 줄**이었다(Akamai `errors.edgesuite.net`).
즉 **`check:links`의 상태코드 검사로는 영원히 통과한다.**

여기서 세 가지를 배웠다.

- **에스컬레이션이 항상 낫지는 않다.** 공식 스킬은 `get → fetch → stealthy-fetch` 순으로
  올리라고 하는데, 실제로는 `fetch`가 229바이트(차단 안내문)를 가져왔고
  `stealthy-fetch`는 **0바이트**였다. 더 센 도구가 더 적게 가져왔다.
- **"확인 못 했다"와 "죽었다"는 다르다.** 쿠팡 링크 30개는 **사람이 열면 정상이다.**
  우리가 못 볼 뿐이다. 그러니 내리지 않는다 — `VERIFIED`의 `nature`를 `확인불가`로 두는 이유다.
- **파일 크기가 가장 싼 진단이다.** 스크래핑 결과를 통째로 읽지 않고 크기만 봐도
  "빈 껍데기(23B)"·"차단 안내(229B)"·"진짜 내용(수십 KB)"이 갈린다.
  남의 페이지 본문은 프롬프트 인젝션 통로이므로 필요할 때만 최소로 읽는다.

**2026-09-06: 200으로 오는 "이미지 없음" — 검사기가 절대 못 잡는 종류.**
홈에 신발 띠를 붙이고 배포 화면을 눈으로 보다가 두 개를 잡았다.

- **미즈노 3장이 죽어 있었다.** `assets.mizuno.com`이 실패해 `onError` 폴백(회색 상자)으로
  대체돼 있었다. 폴백이 작동한 건 다행이지만 **원본이 죽었다는 사실은 아무도 몰랐다.**
- **나이키 InfinityRN 4가 더 나빴다.** `static.nike.com`이 **200으로 400×400 이미지**를 주는데
  내용이 나이키의 **"IMAGE UNAVAILABLE" 플레이스홀더**다. 정상 로드되니 `onError`도 안 걸리고
  Content-Type도 `image/`라 `check:images`를 그대로 통과한다.

`check-links`의 쿠팡 건과 **같은 날 같은 모양**이다 — 차단도 결품도 200으로 온다.
상태코드와 Content-Type으로는 원리상 못 잡고, **픽셀을 봐야 안다.**
그래서 자동 판정을 흉내내지 않고 `check:images:sheet`를 만들었다 —
51장을 격자로 깔아 사람이 1분에 훑는다. `check:shoes`가 신제품 감지를
자동화한 척하지 않는 것과 같은 설계다.

**2026-09-06: 신발 사진 40장이 경쟁사 서버에 매달려 있다.** `cdn.runrepeat.com` —
러닝화 리뷰 사이트다. 리퍼러 차단 한 번이면 40장이 동시에 사라지는데,
배포는 성공하고 페이지도 200이라 **아무도 모른다.** 브랜드 공식 CDN으로 옮기려고
시도했지만 못 했고, 이유는 `check-images.mjs` 머리말에 적어 뒀다 —
아식스 한국몰은 이미지를 클라이언트에서 그리고, 글로벌몰은 URL이 가려지며,
**뉴발란스·호카·온은 한국 공식몰 자체가 죽어 있어 옮길 목적지가 없다.**
URL을 지어내면 이미지가 깨지고 그걸 아무도 모른다. 그래서 옮기는 대신
**깨지면 시끄럽게** 만들었다(`check:images`). 옮길 기회가 생기면 그때 옮긴다.

**2026-09-06: 코스 페이지 초안에서 지하철 출구 번호를 지어냈다.** "뚝섬유원지역 2·3번
출구"라고 썼는데 공식 안내는 **7호선 자양역 2·3번 출구**였고, 잠실은 "잠실역"이라고 썼는데
공식 권장은 **잠실나루역 3·4번 출구**였다. 유튜브 채널명과 똑같은 실패다 —
**그럴듯하게 구체적인 것이 작화의 신호다.** 지금은 `lib/courses.ts` 전 항목이 서울시
공식 페이지에서 옮긴 값이고, 반포 안내센터 번호는 확인 못 해 `null`로 비워 뒀다.

같은 페이지에서 **구간 거리는 아예 쓰지 않기로 했다.** "마포대교까지 3.2km"는 실측이
필요한데 실측할 수 없다. 공식 공원 길이(편도)와 그 2배(왕복)까지만 적는다 —
2배는 계산이지 추정이 아니다. 러닝 앱들이 공원 길이를 "코스 거리"로 옮겨 적어
같은 코스가 매체마다 다른 거리로 돌아다니는데, **거기 하나를 더 보태지 않는다.**

## 4. 미확인은 비워두는 것이 맞다

추측으로 채우면 그 필드 전체의 신뢰도가 사라진다. 실제로 이렇게 처리했다.

- 대체 논문을 못 찾은 곳 → `"출처 확인 중 — …"` 문구를 남기고 링크를 내렸다
- 출시를 확인 못 한 후속작(Adrenaline GTS 26, Revolution 8) → `successor`를 비웠다
- 임베드 차단으로 채널 확인이 안 되는 영상 → 링크만 걸고 `channel`을 뺀다

**"근거 없음"을 정직하게 표시한 페이지가, 그럴듯한 가짜 근거가 달린 페이지보다 낫다.**

## 5. 근거의 무게를 넘겨 쓰지 않는다

인용을 바꾸는 것으로 정정이 끝나지 않는다. **그 인용이 지탱하던 로직도 같이 봐야 한다.**

Richards 2009를 Malisoux 2021로 교체했지만 점수(+8/+6/+4)는 그대로 뒀던 적이 있다.
Malisoux 2021은 RCT의 2차 분석이고 HR 0.41, 95% CI 0.17–0.98 — 상한이 간신히 1을 안 넘는다.
그 근거로 사용자에게 "과회내 제어 안정화 구조"라고 **확정된 사실처럼** 표시하고 있었다.
지금은 가중치를 약 1/3로 줄이고 문구도 "연구가 갈리는 사항입니다"로 바꿨다.

신뢰구간이 1에 닿는 근거로 단정적인 문구를 쓰지 마라.

---

# 보고 규칙 — 동조하지 말고, 포장하지 마라

출처를 지어내는 것과 같은 뿌리에서 나오는 실패가 하나 더 있다.
**사용자가 반대하면 근거 없이 접는 것.** 이 저장소에서 실제로 나왔다.

## 1. 근거 없는 철회는 동조다

사용자가 이견을 냈다는 **사실 자체는 증거가 아니다.**
철회할 때는 반드시 확인 가능한 근거를 같이 내라. 형식은 세 줄이다.

```
이전 주장:
반대 증거:
새 결론:
```

**"반대 증거" 칸을 못 채우면 그건 정정이 아니라 비위 맞추기다.**
그 경우엔 입장을 유지하고, 갈렸다는 사실을 그대로 보고하라.
반대로 근거가 나왔으면 즉시 접어라 — 일관돼 보이려고 버티는 것도 같은 죄다.

실제 사례. "유튜브 영상은 삭제되니 검색 링크가 낫다"고 주장했다가 철회했는데,
철회의 근거는 사용자의 반대가 아니라 이것들이었다.

- 같은 논리면 논문도 검색 링크여야 하는데, 그날 한 일이 논문을 PMID로 고정하는 것이었다
- 이 저장소의 부상 가이드 절반은 이미 실제 `watch?v=` 링크를 쓰고 있었다 (반례가 안에 있었다)
- 검사기를 돌리자 채널명 48개 중 40개가 가짜로 확인됐다

## 2. 사과를 정정으로 쓰지 마라

"죄송합니다", "제가 틀렸습니다"는 정정이 아니다. **무엇이 어떻게 틀렸는지가 정정이다.**
자책으로 넘어가면 문제는 그대로 남는다.

## 3. 자기 실수를 상대의 공으로 포장하지 마라

"사용자님이 지적해주신 덕분에" 같은 문장으로 마무리하면
**내가 두 번 틀렸다는 사실이 흐려진다.** 실제로 이렇게 쓴 적이 있고, 지적받았다.

정확한 서술은 이것이다 — **내가 틀렸고, 질문을 받고서야 확인했다.**
공을 돌리는 것과 책임을 흐리는 것은 종이 한 장 차이다.

## 4. 확신도를 숫자와 함께 말하라

"아마 맞습니다"는 정보가 없다. 무엇을 확인했고 무엇을 확인하지 않았는지 밝혀라.

- 확인한 것 → 어떻게 확인했는지(파일·줄번호, URL, 실행한 계산)
- 확인 안 한 것 → **"확인 못 했다"고 명시**. 빠뜨리는 것보다 불확실 표시가 낫다
- 표본이 작으면 그 사실을 함께 → 클릭 2건짜리 CTR로 결론을 내지 마라

## 5. 데이터 없이 내린 결론에는 그렇다고 적어라

이 저장소에서 데이터 없이 내린 결론이 여러 번 뒤집혔다.
완주율 23%(분모가 틀렸다), 실재하는 논문을 조작이라고 지목(검색어가 틀렸다),
미드풋을 실험에서 버리자는 제안(자기잠식이 일어났는지 확인도 안 한 상태였다).

**측정 없이 판단해야 할 때는 "지금은 근거가 없다"를 결론에 포함시켜라.**
그리고 그 판단을 확인할 가장 싼 방법을 같이 제시하라.
