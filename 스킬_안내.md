# 스킬 안내 — 2026-09-06

**분류 기준은 "무엇을 하는가"가 아니라 "언제 켜지는가"입니다.** 그게 실제로 쓸 때 필요한 정보예요.

스킬은 Claude가 상황에 맞춰 꺼내 읽는 사용설명서입니다. 채팅창에 `/이름`으로 직접 부를 수도
있고, 설명과 맞는 상황이 오면 알아서 꺼내 씁니다. **설명이 정확할수록 오발동이 줍니다.**

---

## A. 항상 배경에서 도는 것

| 스킬 | 하는 일 |
|---|---|
| **lean** | 덜 찾고 · 덜 보내고 · 덜 쓴다 |

세 단계 중 **"덜 쓴다"는 항상 켜져 있습니다.** 첫 질문이 *"이게 존재할 필요가 있나?"* 예요.
계산기 3호를 보류하고, 코스 페이지에서 신발 카드를 뺀 것이 이 원칙입니다.

> 토큰 규약은 이제 `AGENTS.md` 맨 앞에도 박아 뒀습니다. **그쪽이 매 세션 자동으로 들어가므로
> 스킬보다 확실합니다** — 스킬은 상황이 맞아야 켜지지만 `AGENTS.md`는 무조건 들어갑니다.

## B. 상황이 오면 알아서 켜지는 것

| 스킬 | 언제 |
|---|---|
| **grounded** | 논문 인용·신발 스펙·코스 거리처럼 **틀리면 신뢰가 무너지는 것**을 쓸 때 |
| **scrapling-official** | 웹에서 데이터를 긁어야 할 때 · `web_fetch`가 실패할 때 · 봇 차단이 있을 때 |

**`grounded`를 쓰지 말아야 할 때**도 분명합니다 — 잡담, 브레인스토밍, 글 다듬기,
이미 문서가 주어진 질문. 거기서 켜지면 느려지고 장황해지기만 합니다.

**`scrapling-official`은 hyun님 PC에서만 실효가 있습니다.** Claude 작업 환경은 외부를
프록시에서 막아서 요청이 전부 `curl (7) CONNECT tunnel failed, 403`이 됩니다.
명령은 `--ai-targeted`를 **반드시** 붙이세요(프롬프트 인젝션 방어 + 광고 차단).
쓰는 법은 `설정_안내.md` 4-1절에 있습니다.

## C. 사람이 불러야 켜지는 것

| 스킬 | 언제 부르나 | 비용 |
|---|---|---|
| **council** | 되돌리기 비싼 결정 — 도메인 구매, 수익 모델 전환, 사이트 구조 변경 | **토큰 많이** |
| **ai-saas-builder** | "지금 뭐부터 해야 하지?" 우선순위가 헷갈릴 때 | 보통 |

`council`은 여러 전문가 렌즈를 병렬 소집해 이견을 **합치지 않고 표로** 남깁니다.
좋은 도구지만 일상 작업에 쓰면 비쌉니다.

`ai-saas-builder` 진단으로는 이 프로젝트가 **Phase 3(그로스·수익)** 입니다.
그 단계의 경고가 지금 정확히 해당돼요 — **"만들기를 멈추고 재기 시작하라."**

## D. 지금 이 프로젝트와 무관한 것

| 스킬 | 왜 무관한가 |
|---|---|
| **system-prompt-architect** | 시스템 프롬프트 설계용. 우리는 프롬프트를 만들지 않습니다 |
| **prime-agent** | 다른 코딩 에이전트(Prime Agent) 사용법입니다 |

> **이 둘은 지우는 것이 토큰상 이득입니다.** 스킬 설명은 켜지지 않아도 **매 세션 목록에
> 들어갑니다.** 둘 다 영문 설명이고 특히 `prime-agent`가 깁니다.
> 다만 hyun님이 넣으신 것이라 임의로 지우지 않았습니다 — 필요 없으시면 말씀해 주세요.

---

## 설명 언어 — 지금 상태

| 스킬 | 설명 언어 |
|---|---|
| grounded · council · ai-saas-builder | 🇰🇷 한글 (원래 그랬습니다) |
| **lean** | 🇰🇷 한글 **(2026-09-06 교체)** |
| **scrapling-official** | 🇰🇷 한글 **(2026-09-06 교체)** |
| system-prompt-architect · prime-agent | 🇬🇧 영문 (안 쓰는 스킬) |

**`scrapling-official`은 처음에 "비용 대비 얻는 게 없다"며 미뤘는데, 그 판단이 틀렸습니다.**
본문을 다시 저장하는 김에 **이번 주에 실제로 확인한 사실 다섯 개를 스킬 안에 박아 넣었기**
때문입니다 — 이건 문서에 적는 것과 다릅니다. 스킬은 발동할 때 자동으로 읽히니까요.

- 샌드박스에서는 `403 CONNECT tunnel failed`로 막힌다 → 사용자 PC에서 돌려야 한다
- `scrapling` 명령이 PATH에 안 잡히는 게 흔하다 (Windows 경로 포함)
- **에스컬레이션이 항상 낫지 않다** — 쿠팡에서 `stealthy-fetch`가 `fetch`보다 적게 가져왔다
- **차단은 200으로 온다** — 상태코드 말고 **파일 크기**를 봐라(23B/229B/수십KB)
- 스크래핑 본문은 프롬프트 인젝션 통로다 — 크기·개수로 먼저 진단한다

다음에 스크래핑할 때 이 다섯 개를 **다시 발견하지 않아도 됩니다.** 그게 재저장의 진짜 값이었고,
저는 그걸 비용 계산에서 빼먹었습니다.

`system-prompt-architect`·`prime-agent`는 그대로 뒀습니다 — **안 쓰는 스킬이라
설명을 고칠 게 아니라 지울지부터 정하는 게 순서**입니다.

---

## `searchfit-seo` — 2026-09-06 **설치 완료**

무료 SEO 툴킷. 실제로는 **스킬 17개 + 자동 에이전트 3개**가 들어왔습니다(제안 때 본 11개보다 많습니다).

**에이전트 3개가 특히 값집니다** — `seo-auditor`·`content-strategist`·`competitor-analyzer`는
**별도 문맥에서 돌아서** 크롤·파일읽기 과정이 이쪽 대화에 안 쌓이고 결론만 옵니다.
실제로 `seo-auditor`가 **111,610 토큰**을 쓰고 파일 36개를 훑었는데 저한테는 요약 한 덩어리만 왔습니다.
**여러 파일·페이지를 훑어야 답이 나오는 일은 여기로 넘기세요.**

| 스킬 | 한글 이름 | 우리 상황에서 언제 |
|---|---|---|
| `/technical-seo` | 기술 SEO 감사 — 크롤 가능성·색인·속도·코어 웹 바이탈 | **구글 2.7%가 색인 문제인지 순위 문제인지 가른다** ← 여기부터 |
| `/internal-linking` | 내부 링크 구조 분석·개선 | 새로 만든 `/courses`·`/tools`가 색인될 길이 있는지 |
| `/keyword-clustering` | 검색어를 주제별 묶음으로 정리 | **CTR 2%** — 어느 검색어를 노릴지 정한다 |
| `/seo-audit` | 사이트·코드베이스 전체 SEO 종합 감사 | 큰 그림 한 번 |
| `/on-page-seo` | 특정 페이지의 제목·본문·메타 최적화 | 미드풋(CTR 0.6%) 같은 개별 페이지 |
| `/ai-visibility` | ChatGPT·Claude·Gemini·Perplexity 답변에 우리가 나오는지 | 네이버·구글 밖의 새 유입로 |
| `/schema-markup` | JSON-LD 구조화 데이터 생성 | 검색 결과에 별점·FAQ 노출 |
| `/content-strategy` | SEO·자연 유입용 콘텐츠 전략·캘린더 | 다음에 쓸 글 정하기 |
| `/content-brief` | 글 하나의 상세 기획서 | 쓰기 전 설계 |
| `/broken-links` | 죽은 링크 찾기·고치기 | 우리 `check:links`와 겹침 — 그쪽이 더 정밀 |
| `/content-translation` | 다국어 SEO 번역·현지화 | **지금은 불필요** |

**`seo-auditor`를 이미 한 번 돌렸습니다(2026-09-06).** 짚어준 셋을 그날 다 고쳤어요 —
`/shoe-finder`가 크롤러에게 빈 폼이던 것, canonical 부재, compare 고아 18개.
그리고 GSC 실측이 그 진단을 뒷받침했습니다(`유입_설정_기준선.md` 4-2절).

다음은 **`internal-linking`** 입니다 — 색인이 2주 뒤에 안 늘면 그때 씁니다.

> **이 설명은 제가 못 바꿉니다.** 남이 만든 플러그인이라 `save_skill`로 덮어쓸 수 없어서
> 카드에는 계속 영문으로 보입니다. 위 표가 그 대조표입니다.

> **정정 이력.** 이 플러그인을 두고 두 번 틀렸습니다. 처음엔 "설치했다"(사실 아님),
> 그 다음엔 "스킬 이름을 지어냈다"고 정정했는데 **그것도 사실이 아니었습니다** —
> 레지스트리에서 확인하니 이름이 전부 맞았어요. 틀린 건 "설치했다" 하나뿐이었습니다.
> **확인 없이 과하게 정정하는 것도 확인 없이 쓰는 것과 같은 잘못입니다.**

**안 권하는 것.** `brightdata`·`nimble`은 유료 데이터 서비스라 수익 0원인 지금 안 맞고,
`growthbook`은 A/B 테스트인데 **사용자 72명으로는 검정력이 안 나옵니다.**

---

## 자동화 — 모델을 루프에서 뺀다

**토큰을 적게 쓰면서 효과를 키우는 방법은 하나뿐입니다: 모델을 루프에서 빼는 것.**
스크립트가 판정하고 모델은 exit code만 봅니다. 아래 넷은 전부 **토큰 0**으로 돕니다.

| 무엇 | 언제 도나 | 사람이 할 일 |
|---|---|---|
| `npm run check` | 부를 때 · 커밋할 때 | 없음 |
| 커밋 훅 | 커밋마다 자동 | `npm run hooks:on` 한 번 |
| GitHub Actions | 푸시마다 + **매주 월요일** | 없음 (실패 시 메일) |
| `npm run check:images:sheet` | 부를 때 | **눈으로 1분 훑기** |

### ① `npm run check` — 7개를 한 번에

```bash
npm run check       # 오프라인 검사만 (빠름, 커밋 전용)
npm run check:all   # 네트워크 실조회까지 전부
```

**통과한 것은 `✓` 한 글자, 실패한 것만 전체 출력.** 여섯 번 돌려 여섯 번의 출력을 읽던 것이
한 화면이 됐습니다. 사람에게는 번거로움이고 에이전트에게는 토큰입니다.

### ② 커밋 훅 — 규칙을 기억할 필요를 없앤다

```bash
npm run hooks:on    # 저장소당 한 번
```

`AGENTS.md`에 *"커밋 전에 검사기를 돌려라"*고 적혀 있었지만 **문서로는 안 지켜졌습니다.**
(제목 실험 때 "2주간 바꾸지 마세요"를 적어놓고 계속 바꿨던 것과 같은 실패입니다.)
급할 때는 `git commit --no-verify`.

### ③ GitHub Actions — 제가 못 하는 검증을 대신한다

Claude 작업 환경은 외부를 막습니다. 즉 **저는 링크와 사진이 진짜 살아 있는지 확인할 수 없습니다.**
CI 러너에는 네트워크가 있어요.

- **푸시마다** — 오프라인 검사 7개
- **매주 월요일 09시(KST)** — 논문·유튜브·구매 링크·사진을 **실제로 조회**

주간 실행이 필요한 이유: **우리가 안 건드려도 상대가 없앱니다.**
이번 주에 브랜드 공식몰 3곳이 죽어 링크 14개가 함께 죽어 있었어요.

### ④ 사진 대조표 — 기계가 원리상 못 잡는 것

배포 화면을 눈으로 보다가 둘을 잡았습니다. **미즈노 3장이 죽어 폴백 상자로 대체**돼 있었고,
**나이키 1장은 200으로 "IMAGE UNAVAILABLE" 플레이스홀더**를 주고 있었어요.
후자는 정상 이미지로 로드되니 `onError`도 Content-Type 검사도 통과합니다.

```bash
npm run check:images:sheet   # outputs/image-sheet.html 생성
```

51장을 격자로 깔아 **스크롤 한 번**에 전수 확인합니다.
**자동 판정을 흉내내지 않는 것**이 요점이에요 — 픽셀 보는 일은 사람이 하는 게 맞습니다.

### 아직 안 한 자동화 — 신제품 감지

`check:shoes`는 신제품 출시를 스스로 감지하지 못한다고 **스스로 선언해** 뒀습니다.
Scrapling 스파이더로는 가능해지는데, 대상은 **쿠팡이 아니라 다나와·아식스·브룩스·미즈노**입니다
(쿠팡은 Akamai 차단이 확정됐습니다).

다만 hyun님 PC에서 돌아야 하고 유지비가 붙으니, **9/20 판정을 지나고** 만드는 게 맞습니다.
쓰이는지 재지 않고 만든 것은 늘리지 않는다 — 이 저장소의 원칙 그대로입니다.
