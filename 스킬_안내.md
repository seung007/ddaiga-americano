# 스킬 안내 — 2026-09-06

이 프로젝트에서 **어떤 스킬을 언제 쓰는지** 한글로 정리합니다.

스킬은 "Claude가 특정 상황에서 꺼내 읽는 사용설명서"입니다. 채팅창에 `/이름`으로
직접 부를 수도 있고, 상황이 맞으면 Claude가 알아서 꺼내 씁니다.
**설명을 정확히 적어둘수록 알아서 꺼내 쓰는 정확도가 올라갑니다.**

---

## 지금 설치돼 있는 것 (7개)

| 스킬 | 한 줄 | 이 프로젝트에서 언제 |
|---|---|---|
| **scrapling-official** | 웹 스크래핑 — 봇 차단·자바스크립트 렌더링까지 뚫는다 | 쇼핑몰 검색 결과 확인, 신제품 출시 감지, 브랜드몰 이미지 수집 |
| **grounded** | 사실 확인 절차 — 근거·출처를 붙이고 검증한다 | 논문 인용, 신발 스펙, 코스 거리처럼 **틀리면 곤란한 것** |
| **lean** | 토큰·문맥 절약 — 덜 찾고, 덜 보내고, 덜 쓴다 | 큰 파일 다룰 때, 과하게 만들 위험이 있을 때 |
| **council** | 전문가 렌즈 여럿을 병렬로 소집해 이견을 표로 남긴다 | 되돌리기 어려운 결정 앞에서 (**토큰을 많이 씁니다**) |
| **ai-saas-builder** | 아이디어→배포→수익화 단계 진단 플레이북 | "지금 뭐부터 해야 하지?" 우선순위가 헷갈릴 때 |
| **system-prompt-architect** | 시스템 프롬프트 설계·감사 | 이 프로젝트에서는 쓸 자리가 거의 없습니다 |
| **prime-agent** | Prime Agent(오픈소스 코딩 에이전트) 사용법 | 이 프로젝트와 무관합니다 |

### 자세히 — 실제로 쓸 것 5개

#### 🕷️ `scrapling-official` — 이번 주에 막혔던 것들을 푼다

hyun님이 올려주신 저장소 안에 **작성자가 직접 만든 공식 스킬**(v0.4.15)이 들어 있어서
그걸 그대로 등록했습니다. 제가 기억으로 API를 적었으면 또 지어냈을 겁니다.

무엇을 푸나 — 전부 이번 주에 **실제로 막혔던 것**입니다.

- 아식스 한국몰 이미지가 **클라이언트 렌더링**이라 정적 수집이 안 됐다 → 브라우저로 렌더 후 추출
- 29CM·쿠팡 검색 결과를 확인할 수 없었다 → `stealthy-fetch`
- **쿠팡 링크 30개가 미확인**으로 남았다 (안전 정책상 못 엶) → hyun님 PC에서 확인 가능
- **신제품 출시를 자동 감지 못 한다** (`check:shoes`가 포기 선언한 부분) → 스파이더로 브랜드몰 크롤

> ⚠️ **Claude 작업 환경에서는 못 돌립니다.** 설치는 되는데 요청이 전부
> `curl: (7) CONNECT tunnel failed, 403`으로 막힙니다. **hyun님 PC에서 돌려야** 실효가 있습니다.
> 명령이 `scrapling`으로 안 잡히는 문제는 `설정_안내.md` 4-1절 참고(PATH 문제입니다).

명령을 쓸 때는 **`--ai-targeted`를 반드시** 붙이세요. 프롬프트 인젝션 방어이고,
브라우저 명령에서는 광고 차단까지 같이 켜져서 결과가 짧아집니다.

#### 🔍 `grounded` — "확실해?"라고 묻고 싶을 때

이 저장소의 **유일한 차별 주장**이 "근거를 밝히는 추천"입니다. 그 주장을 지키는 스킬이에요.

**쓸 때:** 논문 인용, 신발 스펙·단종 여부, 코스 거리, 시장 수치처럼 **틀리면 신뢰가 무너지는 것**.
**쓰지 말 때:** 잡담, 브레인스토밍, 글 다듬기, 이미 문서가 주어진 질문.
이런 데서 켜면 느려지고 장황해지기만 합니다 — 스킬 설명에도 그렇게 적혀 있습니다.

#### ⚡ `lean` — 토큰이 아까울 때

세 단계로 나뉩니다. **덜 찾고**(코드 검색을 줄임) · **덜 보내고**(긴 출력을 압축) ·
**덜 쓴다**(필요 없는 코드를 안 만듦).

가장 중요한 건 세 번째입니다. 첫 질문이 **"이게 존재할 필요가 있나?"** 예요.
이 프로젝트에서 계산기 3호를 보류하고, 코스 페이지에서 신발 카드를 뺀 것이 그 원칙입니다.

#### 🏛️ `council` — 되돌리기 어려운 결정 앞에서만

여러 전문가 렌즈를 **병렬로** 소집해 이견을 합치지 않고 표로 남깁니다.
"내가 놓친 게 뭐야", "반론 좀", "다각도로" 라고 하면 발동합니다.

> **토큰을 많이 씁니다.** hyun님이 이미 지적하신 부분이라, 일상 작업에서는 쓰지 않습니다.
> 도메인 구매, 수익 모델 전환, 사이트 구조 변경처럼 **되돌리기 비싼 결정**에만 쓰세요.

#### 📈 `ai-saas-builder` — "지금 뭐부터?"

지금 어느 단계인지 진단하고 그 단계 플레이북만 골라 적용합니다.

이 프로젝트 진단은 **Phase 3 (그로스·수익)** 입니다 — 배포는 됐고 유입·매출이 필요한 단계.
그리고 이 단계의 핵심 경고가 지금 우리에게 정확히 해당합니다:
**"만들기를 멈추고 재기 시작하라."**

---

## 아직 설치 안 된 것 — `searchfit-seo` (무료)

제안만 해둔 상태이고 **아직 설치되지 않았습니다.** (2026-09-06 `list_plugins` 확인)

무료 SEO 툴킷이고 스킬 11개짜리입니다. 지금 1순위인 **Google Search Console 등록** 바로
다음에 쓸 것들이에요.

| 스킬 | 왜 지금 필요한가 |
|---|---|
| `technical-seo` | **구글 유입 2.7%**가 색인 문제인지 순위 문제인지 가른다 |
| `internal-linking` | 새로 만든 `/courses`·`/tools`가 색인되려면 내부 링크가 필요하다 |
| `keyword-clustering` | **CTR 2%** — 어느 검색어를 노릴지 정한다 |
| `ai-visibility` | ChatGPT·Claude 답변에 우리 사이트가 나오는지 |
| `on-page-seo`·`schema-markup`·`seo-audit`·`content-brief`·`content-strategy`·`broken-links`·`content-translation` | 나머지 7개 |

> **정정 이력.** 저는 이 플러그인을 두고 두 번 틀렸습니다. 처음엔 "설치했다"고 적었고
> (사실 아님), 그 다음엔 "스킬 이름을 지어냈다"고 정정했는데 **그것도 사실이 아니었습니다** —
> 레지스트리에서 확인하니 이름이 전부 맞았어요. 틀린 건 "설치했다" 하나뿐이었습니다.
> **확인 없이 과하게 정정하는 것도 확인 없이 쓰는 것과 같은 잘못입니다.**

**안 권하는 것도 적습니다.** `brightdata`·`nimble`은 유료 데이터 서비스라 수익 0원인
지금은 안 맞고, `growthbook`은 A/B 테스트인데 **사용자 72명으로는 검정력이 안 나옵니다.**

---

## 자동화 — 모델을 루프에서 뺀다

토큰을 적게 쓰면서 효과를 키우는 방법은 하나뿐입니다.
**자동화는 스크립트가 하고, 모델은 판정만 본다.**

검사기 6개는 이미 그 형태입니다 — 노드 스크립트라 **토큰이 0**이고, Claude는 exit code만 봅니다.
오늘 세 가지를 더 붙였습니다.

### ① `npm run check` — 검사기 7개를 한 번에, 요약 한 줄로

```bash
npm run check       # 오프라인 검사만 (빠름, 커밋 전용)
npm run check:all   # 네트워크 실조회까지 전부
```

지금까지는 검사기를 여섯 번 따로 돌리고 여섯 번의 출력을 읽었습니다.
**통과한 검사기의 출력은 읽을 이유가 없는데도 매번 맥락에 들어왔어요.**
그래서 뒤집었습니다 — **통과한 것은 이름과 ✓만, 실패한 것만 전체 출력.**

### ② 커밋 훅 — 사람이 기억할 필요를 없앤다

```bash
npm run hooks:on    # 저장소당 한 번만
```

`AGENTS.md`에 *"출처를 건드렸으면 커밋 전에 검사기를 돌린다"*고 적혀 있는데,
**문서에 적는 것으로는 안 지켜졌습니다.** (제목 실험 때 "2주간 바꾸지 마세요"를
적어놓고 계속 바꿨던 것과 같은 실패입니다.) 훅을 켜면 규칙을 기억할 필요가 없어집니다.

급할 때는 `git commit --no-verify`로 건너뜁니다.

### ③ GitHub Actions — 제가 못 하는 검증을 대신한다

`.github/workflows/checks.yml`. 이게 **가장 큰 것**입니다.

Claude 작업 환경은 외부 사이트를 프록시에서 막습니다 — `check:images` 51장이 전부
`fetch failed`였고 Scrapling도 403으로 막혔습니다. 즉 **저는 링크와 사진이 진짜로
살아 있는지 확인할 수 없습니다.** GitHub Actions 러너에는 네트워크가 있어요.

- **푸시할 때마다** — 오프라인 검사 7개
- **매주 월요일 09시(KST)** — 인용 논문·유튜브·구매 링크·신발 사진을 **실제로 조회**

주간 실행이 왜 필요한가. 우리가 아무것도 안 건드려도 **상대방이 없앨 수 있기 때문**입니다.
이번 주에 실제로 브랜드 공식몰 3곳이 죽어 링크 14개가 함께 죽어 있었습니다.

> 푸시마다 남의 서버를 두드리면 민폐라, 네트워크 조회는 **주 1회와 수동 실행에서만** 돕니다.
> 실패하면 GitHub이 hyun님 메일로 알립니다.

### ④ `npm run check:images:sheet` — 기계가 못 잡는 것을 사람이 1분에

배포 직후 홈 화면을 눈으로 보다가 두 개를 잡았습니다. **미즈노 3장이 죽어 있었고**,
**나이키 1장은 200으로 "IMAGE UNAVAILABLE" 플레이스홀더**를 주고 있었어요.
후자는 정상 이미지로 로드되기 때문에 **검사기가 원리상 못 잡습니다.**

그래서 51장을 한 화면에 격자로 까는 HTML을 만듭니다. 스크롤 한 번이면 전수 확인이 끝나요.
**자동 판정을 흉내내지 않는 것**이 요점입니다 — 픽셀을 보는 일은 사람이 하는 게 맞습니다.

### 아직 안 한 자동화 — 신제품 감지

`check:shoes`는 **신제품 출시를 스스로 감지하지 못한다**고 스스로 선언해 뒀습니다.
브랜드마다 공지 방식이 다르고 공개 API가 없어서요.

Scrapling 스파이더로는 **가능해집니다.** 다만 hyun님 PC에서 돌아야 하고 유지비가 붙으니,
**쿠팡 확인이 실제로 되는 걸 본 다음에** 만드는 게 맞습니다.
쓰이는지 재지 않고 만든 것은 늘리지 않는다 — 이 저장소의 원칙 그대로입니다.
